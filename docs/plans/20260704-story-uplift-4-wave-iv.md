# Story uplift 4/5 — волна IV: модули 09–10 (Акт III «Объясняешь ты»)

## Overview

Пятый план семейства story-uplift (архив исходного плана —
`docs/plans/completed/20260703-story-uplift.md`). Раскатка сюжетного слоя на
модули 09 и 10 — 10 юнитов Акта III: переворот ролей. Приходит джун Олег,
наивные вопросы мигрируют к нему, отвечает читатель; финал — кольцо 10-05
(★★, две сцены в одном юните) с передачей CDC-потока в kafka-cookbook.

**Предусловие запуска:** план `3-wave-iii` завершён и принят владельцем.

Ключевые биты волны (детали — в записях карты сцен): ввод Олега (модуль 09),
спор менторов (09-05), переворот ролей и кольцо катехизиса (10-05).

## Context

- Канон истории: `docs/story-canon.md` — целиком перед каждой правкой прозы;
  Олег — §2.8, кольцо — §1.3, финальные payoff'ы — §7.
- Карта сцен: `docs/story-scene-map.md` — запись юнита = обязательный вход
  задачи.
- Гейты сцен: `.claude/skills/lecture-writer/references/checklist.md`.
  Скилл и линтер лежат в гитигноренном `.claude/` — артефакт локальной
  рабочей копии, в свежем клоне репозитория их нет: волны запускаются
  из этой рабочей копии.
- Модули волны: `lectures/09-writes-eventing-and-server-logic/`,
  `lectures/10-use-cases/` — правятся только `i18n/{ru,en}/README.md`.
- Капстоны модуля 10 — большие юниты с интеграционными тестами; тесты и код
  не трогаются, правится только README-проза.
- Предусловия окружения: команды из корня; `pnpm install` выполнен;
  Docker-песочница не нужна.

## Development Approach

- Одна задача = один юнит (RU+EN вместе), полностью до перехода дальше;
  внутри волны — по номерам.
- Правки — только `i18n/{ru,en}/README.md`. Код, `query.sql`, `sqlc.yaml`,
  `Makefile`, тесты капстонов, секции `## Запуск` / `## Running it`
  неприкасаемы.
- Полные сцены — только у ★ по карте; 10-05 — ★★ (два сценовых слота,
  санкционировано §4.2). Сцена заменяет прозу (cap ×1.5), правило отката
  действует.
- Акт III: владение концептом у читателя — в сценах объясняешь ты (короткими
  формулами, не лекциями — §3.1); наставники отступают на реплики-вердикты.
- Гейты зелёные — до следующей задачи. CRITICAL: при отклонении объёма
  обновлять план (➕/⚠️).
- Коммит на задачу: `docs:` — EN subject, RU body (формат по CLAUDE.md).

## Testing Strategy

- После каждого юнита — «линтер юнита» (команда в Technical Details).
- `make web-check-coverage` / `make web-build` — на чеке волны.
- Консистенси-чек волны: греп нитей §7 (в этой волне закрывается большинство
  payoff'ов), сверка голосов §2, полный линтер по двум модулям.

## Progress Tracking

- Отмечать `[x]` сразу; новые пункты — ➕, блокеры — ⚠️.
- При расхождении с планом — править план в том же коммите.

## Technical Details

- «Линтер юнита» = `node .claude/skills/lecture-writer/scripts/check_unit.mjs
  <путь-юнита> --against-head` (из корня; путь — в блоке Files задачи).
- Порядок работ: канон целиком → запись карты → текущие README → RU → EN →
  линтер → diff-скоуп.
- Гости финала (Kafka-команда в 10-05) — строго безымянны: межрепозиторных
  обязательств нет, Debezium — безличная точка стыковки.
- Байт-компат канона Brew в репликах: правило «переименуем колонку — сломаем
  им Debezium» звучит голосом читателя (10-05), сами таблицы и код не
  упоминать сверх записи карты.

## Implementation Steps

### Task 1: юнит `09-01-merge-and-copy` — микро-диалог

**Files:**
- Modify: `lectures/09-writes-eventing-and-server-logic/09-01-merge-and-copy/i18n/ru/README.md`
- Modify: `lectures/09-writes-eventing-and-server-logic/09-01-merge-and-copy/i18n/en/README.md`

Особое: модуль 09 вводит Олега — где именно он появляется впервые, определяет
запись карты (свериться с §2.8 и §6 канона до правки).

- [x] прочитать канон целиком, запись `09-01` в карте сцен, оба README юнита
- [x] RU: микро-диалог по записи карты; гейты — чек-лист скилла lecture-writer
- [x] EN: зеркало — число реплик именованных персонажей равно RU, без he/she
  о читателе
- [x] линтер юнита — зелёный; diff юнита — только два README

### Task 2: юнит `09-02-skip-locked-job-queue` — микро-диалог

**Files:**
- Modify: `lectures/09-writes-eventing-and-server-logic/09-02-skip-locked-job-queue/i18n/ru/README.md`
- Modify: `lectures/09-writes-eventing-and-server-logic/09-02-skip-locked-job-queue/i18n/en/README.md`

- [x] прочитать канон целиком, запись `09-02` в карте сцен, оба README юнита
- [x] RU: микро-диалог по записи карты
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README

### Task 3: юнит `09-03-transactional-outbox` — ★ полная сцена

**Files:**
- Modify: `lectures/09-writes-eventing-and-server-logic/09-03-transactional-outbox/i18n/ru/README.md`
- Modify: `lectures/09-writes-eventing-and-server-logic/09-03-transactional-outbox/i18n/en/README.md`

Особое: payoff outbox-тизера Ботыра из 00-01 («лучшая наша таблица») и эхо мема
«просто»; владелец концепта outbox — этот юнит (10-05 не переобъясняет);
дедуп называется «по `outbox_id` (таблица `processed_outbox_ids`)» — жёсткий
вход §9.4 канона подачи.

- [x] прочитать канон целиком, запись `09-03` в карте сцен, оба README юнита
- [x] RU: сцена по записи карты (cap ×1.5, сцена заменяет прозу боли)
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README
- ➕ Опциональный перекур карты («стоп, а почему не exactly-once?») отложен:
  ответ (дедуп на приёме по `outbox_id` / `processed_outbox_ids`) уже
  раскрыт прозой в таблице гарантий и заборчике, а вставка диалога в тело
  теории (§5.3) без существующей самопроверки давала лишний риск; ★
  сцена-открытие несёт все обязательные нити (§7: мем «просто», Олег,
  outbox «лучшая наша таблица») и вписана в cap ×1.5 (RU 197/132 = 1.49×).

### Task 4: юнит `09-04-listen-notify` — микро-диалог

**Files:**
- Modify: `lectures/09-writes-eventing-and-server-logic/09-04-listen-notify/i18n/ru/README.md`
- Modify: `lectures/09-writes-eventing-and-server-logic/09-04-listen-notify/i18n/en/README.md`

Особое: эпизод нити «war-room пула» (00-06 → 05-06 → 09-04 → 10-04, реестр §7).

- [x] прочитать канон целиком, запись `09-04` в карте сцен, оба README юнита
- [x] RU: микро-диалог по записи карты
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README

### Task 5: юнит `09-05-triggers-and-volatility` — ★ полная сцена (спор менторов)

**Files:**
- Modify: `lectures/09-writes-eventing-and-server-logic/09-05-triggers-and-volatility/i18n/ru/README.md`
- Modify: `lectures/09-writes-eventing-and-server-logic/09-05-triggers-and-volatility/i18n/en/README.md`

Особое: спор менторов — легальное столкновение позиций (у каждого своя
правда, §2 канона); личная ставка читателя (`updated_at`) остаётся за
читателем.

- [x] прочитать канон целиком, запись `09-05` в карте сцен, оба README юнита
- [x] RU: сцена по записи карты (cap ×1.5)
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README
- ➕ Реализовано по записи карты полностью: сцена-открытие (payoff нити «Спор
  Павел/Дмитрий» §7 — граница рождается при читателе, крисп-правило остаётся в
  Заборчике по §5.7), перекур с военной историей Павла «IMMUTABLE, который врал»
  (одна на модуль §2.2) и мостик Эмиля (в чате) о запуске лояльности → 10.
  Личная ставка `updated_at` — за читателем (§3.3), ремарка настоящим временем.
  Cap ×1.5 держит открытие: RU 149/101 = 1.48×, EN 186/128 = 1.45×
  (перекур и мостик — аддитивные слоты §4.1, под кап не попадают). Линтер
  зелёный (0 ошибок); 5 предупреждений разобраны — все на строках-продолжениях
  реплик Павла (`журнал` — существительное; `писал`/`поверил`/`должна` —
  согласование с не-читательскими подлежащими), пол читателя не задет.

### Task 6: юнит `10-01-build-the-brew-schema-capstone` — ★ полная сцена

**Files:**
- Modify: `lectures/10-use-cases/10-01-build-the-brew-schema-capstone/i18n/ru/README.md`
- Modify: `lectures/10-use-cases/10-01-build-the-brew-schema-capstone/i18n/en/README.md`

Особое: капстон применяет ретрай-петлю и транзакционную дисциплину (ссылки на
05-05/03-03 — прозой, без переобъяснения).

- [x] прочитать канон целиком, запись `10-01` в карте сцен, оба README юнита
- [x] RU: сцена по записи карты (cap ×1.5)
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README
- ➕ Сцена-открытие «планёрка лояльности» по записи карты: Ботыр и ты ведёте
  капстон на равных (payoff арки Ботыра §7 — сам приводит правило постмортема
  07-02 «поле с инвариантом — колонка»), Олег на подхвате роняет унаследованный
  мем «просто» (эхо отыгранной нити, не новый setup), ты отражаешь его формулой
  одного предложения (Акт III §3.1), Дмитрий закрепляет вердиктом «инвариант —
  в схему, ретрай — в клиент». Четыре риска открытия (float-дрейф, дубль email
  от двух бэкендов, скан журнала, гонка бонус/ночной job) выжили: два — в первой
  реплике Ботыра, два — в прозе-мостике сразу после сцены (§гейт-заметки карты).
  Бариста и ночной job — роли без реплик (§2.9); чисел stdout в репликах нет
  (§5.4); владение SERIALIZABLE/CHECK/ключа не мигрирует — сцена подводит,
  теория ссылается «как в 05-05» (§5.7). Cap ×1.5 держит: RU 157/127 = 1.24×,
  EN 198/149 = 1.33×. Линтер зелёный (0 ошибок); 1 предупреждение — «журнала» в
  ремарке-мостике (родительный падеж существительного «журнал», не глагол о
  читателе; тот же класс ложных срабатываний, что в Task 5), пол читателя не
  задет.

### Task 7: юнит `10-02-price-and-promo-engine` — микро-реплика

**Files:**
- Modify: `lectures/10-use-cases/10-02-price-and-promo-engine/i18n/ru/README.md`
- Modify: `lectures/10-use-cases/10-02-price-and-promo-engine/i18n/en/README.md`

Особое: payoff нити «деньги = BIGINT-центы под защитой» (01-01 → 02-04 → 10-02).

- [x] прочитать канон целиком, запись `10-02` в карте сцен, оба README юнита
- [x] RU: микро-реплика по записи карты
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README
- ➕ Реализовано по записи карты: микро-реплика (ступень 3, два вплетённых в
  прозу открытия blockquote'а — образец §9.3). Руслан (в чате, 09:03) —
  телеграф с временем и числом (маркер §2.6: «Касса. Латте. Две цены»); Евгений
  сам произносит payoff арки «Обучение Евгения» (§7): «инвариант — в схему, я
  себе не доверяю», предварительно признавая, что наслоившееся окно `SUMMER` —
  его. Обе канонные боли выжили прозой: две цены на день (менеджер — безымянная
  роль, реплик не получает) и наслаивающееся окно промо. Аудит «было→стало» /
  RETURNING old/new и перекличка с журналом регулятора 09-05 остались прозой,
  без переобъяснения (владелец RETURNING — модуль 03). Нить «деньги =
  BIGINT-центы под защитой» (01-01→02-04→10-02, канон подачи §3) задета прозой:
  `price_cents bigint NOT NULL CHECK (price_cents > 0)` в DDL — payoff уже в
  описываемом прозой коде, отдельная строка не нужна (§7-грепа у нити нет).
  Микро-форма не раздута до сцены (гейт-заметка карты): добавлено ~35 слов,
  ×1.5-кап заменяемой прозы для ступени 3 не применяется (реплики аддитивны по
  §4.2). Линтер зелёный (0 ошибок); 1 предупреждение — «правила» на строке 18
  RU (существительное «rules» мн.ч. в теорфразе «Такие правила нельзя держать»,
  окончание -ла), ложное срабатывание -ла-эвристики на не-читательском
  подлежащем; пол читателя не задет (тот же класс, что «журнал» в Task 5 /
  «журнала» в Task 6). Число именованных реплик RU==EN=2, читательских 0==0.

### Task 8: юнит `10-03-app-anti-patterns-clinic` — микро-реплика

**Files:**
- Modify: `lectures/10-use-cases/10-03-app-anti-patterns-clinic/i18n/ru/README.md`
- Modify: `lectures/10-use-cases/10-03-app-anti-patterns-clinic/i18n/en/README.md`

Особое: личная ставка читателя (N+1 в свежем PR) — остаётся за читателем.

- [x] прочитать канон целиком, запись `10-03` в карте сцен, оба README юнита
- [x] RU: микро-реплика по записи карты
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README
- ➕ Реализовано по записи карты в две микро-формы. (1) Ступень 3: почти-реплика
  безымянного DBA в открытии поднята до именованной цитаты Павла в тикете, строчными
  (маркер §2.6/§4.4): «pg_stat_statements смотрел. запросы простые. база здорова.
  лечите код.» — склейка «твой DBA = Павел» (§2.2); текущее сильное открытие («база
  здорова, дашборд красный», клиника/болезнь/лечение, пять болезней, симптом
  «сороковая страница по таймауту») сохранено — полной сценой НЕ заменяется (правило
  отката §4.3, звезды нет). (2) Ступень 2: перекур на шве после теорблока N+1 (не
  внутрь теории, §5.3) — N+1 в ТВОЁМ свежем PR (личная ставка §3.3, балансировка
  Ботыра §2.3, явно «не из легенд про чужой код»); Дмитрий не разносит, а считает
  round-trip'ы вместе (сократика + нормализация ошибки), рефрен катехизиса «что база
  ответила» (§7, эхо — payoff в 10-05). Владение не мигрирует (§5.7): keyset 03-02 /
  non-sargable 06-03 — только применение прозой. Реплик читателя нет пересказа stdout
  (§5.4): reader считает СТРУКТУРУ (1+N — «на одну больше, чем клиентов»), не число
  вывода. Числа реплик RU==EN: именованные 3 (Павел 1 + Дмитрий 2), читателя 1==1.
  Линтер зелёный (0 ошибок); 1 предупреждение — «ответила, вернул» на строке-
  продолжении реплики Дмитрия (подлежащие «база»/«батч», не читатель; известный
  предел линтера — continuation-строки multi-line реплики сканируются как ремарки,
  тот же класс, что в Task 5/6/7), пол читателя не задет. Перекур — аддитивный слот
  §4.1, под кап ×1.5 не попадает; микро-реплика не раздута до сцены (гейт-заметка
  карты).

### Task 9: юнит `10-04-pooling-from-the-app` — микро-диалог

**Files:**
- Modify: `lectures/10-use-cases/10-04-pooling-from-the-app/i18n/ru/README.md`
- Modify: `lectures/10-use-cases/10-04-pooling-from-the-app/i18n/en/README.md`

Особое: финал нити «war-room пула» — дежурство читателя (PgBouncer); эхо
00-06 прозой.

- [x] прочитать канон целиком, запись `10-04` в карте сцен, оба README юнита
- [x] RU: микро-диалог по записи карты
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README
- ➕ Реализовано по записи карты в две микро-формы. (1) Открытие — чат-жанр §4.4:
  первое ночное дежурство читателя (payoff нити «War-room пула» §7 —
  00-06→05-06→09-04→10-04). После деплоя pgbouncer в 03:11 сыплются алерты, ты
  пишешь Павлу в 03:14 назывными/без рода («Лок висит. LISTEN молчит. prepared
  падает. Что общего?» — канонизированное сообщение в 03:14, §2.2), Павел
  отвечает фирменными строчными («пул. транзакционный. сессии больше нет.» —
  §4.4). Обезличенный список симптомов заменён сценой; завязка «Brew вырос →
  max_connections → pgbouncer transaction mode» и структура трёх поломок
  (advisory-локи / LISTEN / prepared) сохранены (гейт-заметка). (2) Перекур
  §4.1 (второй в модуле — санкционирован, 5 тяжёлых уроков ≈ 2 перекура): утреннее
  ретро на шве после теорблока «Поломка 3», не внутрь теории (§5.3) — Павел
  формулой «пул даёт бэкенд на транзакцию, не на сессию» подводит к таблице
  фиксов (§5.7: механику объясняет теория юнита, реплики Павла лишь подводят),
  Дмитрий переводит в план действий; совмещено с самопроверкой «назови три
  вещи — и почему каждая», reader считает СТРУКТУРУ (три session-scoped вещи),
  не число вывода (§5.4). Мостик в 10-05 («две книги сходятся на одной модели
  данных») сохранён прозой; Заборчик оставлен буллетами (ступень 5 опциональна).
  Гендер-линт: реплики «Ты» в панике — назывные/настоящее время; попутно
  масккулинная проза «Закрыл… вернулся» в абзаце-2 (теперь в границе сцены)
  переведена в настоящее время 2 л. «Закрываешь… возвращается» (§3.2). Cap ×1.5
  держит открытие: RU cold-open 170/138 = 1.23× (перекур — аддитивный слот §4.1,
  под кап не попадает). Числа реплик RU==EN: именованные 4 (Павел 2 + Дмитрий 2),
  читателя 2==2. Линтер зелёный (0 ошибок); 3 предупреждения — «пул»/«котёл» в
  абзаце-2 (существительные м.р. на -л в теорпрозе, попавшей в границу сцены;
  тот же класс ложных срабатываний -л-эвристики, что «журнал»/«журнала» в
  Task 5–8), пол читателя не задет.

### Task 10: юнит `10-05-the-cdc-seam-handoff` — ★★ две сцены (кольцо курса)

**Files:**
- Modify: `lectures/10-use-cases/10-05-the-cdc-seam-handoff/i18n/ru/README.md`
- Modify: `lectures/10-use-cases/10-05-the-cdc-seam-handoff/i18n/en/README.md`

Особое: два сценовых слота (открытие + финальное кольцо в концовке —
отступление от обычного слота санкционировано ★★); payoff'ы всех главных
нитей §7 (катехизис — произносишь ты Олегу, блокнот Павла — «Слот твой», чек
заказа #1, салфетка-итог Дмитрия); прощание с читателем и мета-рефлексия о
SQL-протагонисте обязаны выжить; форма глагола прощания — по решению
открытого вопроса №2 (см. карту сцен); Kafka-команда безымянна; RU/EN-инвариант
реплик критичен — реплик много.

- [x] прочитать канон целиком, запись `10-05` в карте сцен, оба README юнита
- [x] RU: обе сцены по записи карты (cap ×1.5 на каждый заменяемый фрагмент)
- [x] EN: зеркало — число реплик именованных персонажей равно RU
- [x] линтер юнита — зелёный; diff юнита — только два README
- ➕ Реализованы обе сцены ★★ по записи карты. (1) Сцена-открытие (замена
  холодного открытия): Евгений приводит безымянную Kafka-команду (payoff арки
  «Обучение Евгения» §7), Олег наследует «стоп, а почему не outbox с relay, как
  раньше?» — но отвечаешь уже ты (переворот ролей Акта III §3.1). Различие
  механизмов названо по §1.4 канона подачи (outbox — доставка руками vs CDC —
  Postgres пишет её сам): владелец outbox 09-03 не переобъясняется, реплика лишь
  контрастирует. PUBLICATION/REPLICA IDENTITY — ремаркой-прозой (владелец —
  этот юнит, теория объясняет; §5.7), byte-compat — твоей короткой репликой
  («переименуем колонку — сломаем им Debezium», §3.1). Бариста/маркетинг —
  роли прозы без реплик (§2.9). (2) Сцена-кольцо в концовке (★★ санкционирует
  отступление от слота-мостика, §4.1 слот-4): блокнот Павла (payoff — делегирует
  периметр «Слот твой. Следи за диском»), чек заказа #1 Эмиля («первый заказ
  уезжает в большой мир»; данные-персонаж Алиса Иванова упомянута, без реплики),
  катехизис Дмитрия (формулу «Покажи запрос» произносишь ты — Олегу, кольцо
  §1.3), Олег закрывает кольцо, последняя реплика курса — салфетка-итог Дмитрия
  (payoff «Салфетки Дмитрия»). Хук застрявшего слота → No space left — реплика
  Павла лишь подводит, объяснение остаётся в Заборчике/теории. Мета-рефлексия о
  SQL-протагонисте и прямое прощание сохранены (не вытеснены сценами); форма
  прощания — нейтральный кандидат открытого вопроса №2 карты («Это был последний
  юнит. Спасибо тебе за этот год в Brew»), маскулинное «дошёл» устранено, EN
  зеркалит («Thank you for this year at Brew»). Kafka-команда строго безымянна
  (§6). Числа реплик RU==EN: именованные 6, читателя 3 (линтер без mismatch).
  Cap ×1.5: RU открытие 187/127 = 1.472×, EN 227/152 = 1.493×; сцена-кольцо —
  аддитивный слот финала, заменяет лишь фразу прощания. Линтер зелёный (0
  ошибок); 4 предупреждения — известный класс -л/-ла: «обещал» (строка-
  продолжение реплики Евгения), «стол»/«павла»/«был» (существительное / имя в
  род. падеже / м.р. о слове «юнит»), пол читателя не задет. `web-check-coverage`
  зелёный (63/63 RU, 63/63 EN, mismatches 0).

### Task 11: Verify acceptance criteria (консистенси-чек волны IV)

**Files:**
- Modify: `docs/story-canon.md` (пометки статуса нитей §7)

- [ ] полный линтер волны: `node
  .claude/skills/lecture-writer/scripts/check_unit.mjs
  lectures/09-writes-eventing-and-server-logic lectures/10-use-cases
  --against-head` — ноль ошибок, предупреждения разобраны
- [ ] нити §7: греп по греп-маркерам (RU и EN); после этой волны нитей в
  состоянии «setup без payoff» быть не должно — несоответствия исправить или
  явно отметить в §7 канона с причиной
- [ ] сверка голосов: все сцены волны подряд «глазами» каждого говорившего
  персонажа против реестра §2 (включая Олега — §2.8); отклонения поправить
- [ ] `make web-check-coverage` и `make web-build` — зелёные
- [ ] скоуп прогона: каждый коммит прогона (все они правят этот план-файл;
  список — `git log --format=%h --
  docs/plans/20260704-story-uplift-4-wave-iv.md`) под `lectures/` меняет
  только `i18n/{ru,en}/README.md` юнитов волны (проверить `git show --stat`)
- [ ] все требования Overview выполнены (10 юнитов, переворот ролей и кольцо
  на месте)

## Post-Completion

*Ручные шаги владельца — без чекбоксов.*

- Приёмка волны: чтение финала (09-05, 10-05 — обе сцены, RU и EN) —
  эмоциональная кульминация курса, кринж-тест строже обычного.
- После приёмки волны — переместить этот план в `docs/plans/completed/`
  (`git mv docs/plans/20260704-story-uplift-4-wave-iv.md docs/plans/completed/`).
- Дальше: `ralphex docs/plans/20260704-story-uplift-5-final-pass.md`.

# План: сюжетная доработка курса (story uplift)

> **Дата:** 2026-07-03 · **Статус:** к ратификации владельцем (Фаза 0)
> **Канон-источник:** [`docs/story-canon.md`](../story-canon.md) — читать первым,
> план не дублирует его содержание.
> **Связь с прошлым планом:** [`20260611-pedagogy-uplift.md`](20260611-pedagogy-uplift.md)
> остаётся в силе для Тиер-0/1/2 (интерактив, глоссы, самопроверки); его
> решение «функции, не персонажи» **отменяется** этим планом (см. Фазу 0.1).
> Тиер-0-раскатка и story-uplift не конфликтуют: их правки в юните совместимы
> и могут идти одним проходом.

---

## 1. Контекст и цель

Владелец сравнил курс с Яндекс Практикумом (спринты 1–10, корпус
`temp/yandex/`) и хочет: **сквозную историю с прописанными персонажами и
диалогами**, чтобы в каждом модуле/уроке читатель сталкивался с проблемой и
решал её вместе с командой. Кофейня Brew и структура знаний неизменны.

Что показало исследование (20 агентов по корпусу, детали в
`temp/ai/pedagogy/` и сводках сессии):

- У Практикума ситком живёт в спринтах 1–4 (команда GO-Sasha/GO-Liza/GO-Gosha,
  «Студент» с кнопками-репликами, продукт-заказ) и **демонтирован** во второй
  половине (спринты 8–10 — ноль реплик). Причина: сцены требовали топлива,
  не связанного с материалом (быт, гэги), и квота «сцена в каждом уроке»
  оказалась невыполнимой.
- Сильнейшие приёмы эталона: проблема-до-теории, «Обучаем Алису» (сквозной
  растущий демо-проект), чёткое ролевое разделение персонажей, ритуалы.
  Фатальные ошибки: брошенные клиффхэнгеры, разрыв истории и реальной работы
  ученика, персонажи без арок, каламбуры.
- Наш курс уже имеет линзу «ты — бэкенд Brew», боли-открытия и якоря
  (канон подачи `course-canon.md`), но людей и диалогов в нём нет — вся
  «команда» безлична («твой DBA», «маркетинг захотел»).

**Цель плана:** надстроить над каноном подачи слой истории — команду с
характерами, сценами и арками — не ломая ни одного гейта (байт-точный
stdout, неприкасаемый код, RU/EN-зеркала, валидация движка) и не повторив
аварию Практикума.

## 2. Как выбирали историю

Конкурс: 4 независимых концепта → 3 судьи (методист, шоураннер, хранитель
канона).

| Концепт | Методист | Шоураннер | Хранитель | Σ |
|---|---|---|---|---|
| **A «Команда Brew: второй этаж»** | 8 | 8 | 6 | **22** |
| B «Хроника роста» | 7.5 | 6.5 | 8 | 22 |
| C «Дежурство по данным» | 6 | 7 | 5 | 18 |
| D «Передача смены» | 7 | 7.5 | 6.5 | 21 |

**Победитель — A** (лучшая педагогика: полный набор функций у именованных
носителей, включая peer-персонажа с аркой искупления; лучшая драматургия:
голоса различимы синтаксически, конфликты с настоящими ставками). Претензии
хранителя канона к A устранены прививками:

- из **B**: бюджет 2–3 сцены/модуль и «худший случай деградации = сегодняшний
  курс»; regexp-линт гендер-нейтральности; cap ×1.5 на длину сцены; реестр
  маркеров голоса RU/EN; камео Анна (= staff-Анна, с ратификацией) и Паша;
- из **C**: «диалог без педагогической функции не пишется»; «реплики не
  пересказывают stdout»; плашка-сводка как нижняя ступень; правило отката
  «сцена обязана быть сильнее существующего открытия»;
- из **D**: впрыск джуна Тимура в модуле 09 (наивный вопрос мигрирует к нему,
  читатель отвечает); кольцевая композиция (катехизис Марата 00-01 → финал
  10-05); правило «шутка обязана переживать перевод»; гармонизация
  маскулинной прозы 00-01.
- вето хранителя на D-механику «Марат уходит в команду по ту сторону
  CDC-шва» учтено: гости финала — безымянная Kafka-команда, межрепозиторных
  обязательств нет.

Правки A по вердиктам: обязательный ритуал «Зоя — в ревью» на каждом
Заборчике понижен до опционального приёма (иначе полный проход 126 файлов);
личные ставки читателя (02-06 ALTER, 09-05 `updated_at`, 10-03 N+1) остаются
за читателем; `UPDATE` без `WHERE` (03-03) — флешбэк Дани из времени до
прихода читателя (канонная атрибуция «разработчик» не нарушена).

Итог синтеза зафиксирован в **`docs/story-canon.md`**: каст (Марат, Зоя,
Даня, Стас, Виктор + камео Анна, Паша, Тимур), три акта с переворотом ролей,
бюджет ~20 полных сцен, лестница деградации, 10 правил гигиены, помодульные
биты, реестр нитей setup→payoff, брифы аватаров, 5 образцовых сцен.

## 3. Фаза 0 — предусловия (блокеры, без них не стартуем)

Все три судьи независимо назвали одно и то же общее вето: конфликт с
действующим правилом «функции, не персонажи». Пока оно не снято, любой
агент-переписчик будет **выпиливать** сцены по действующей инструкции.

- [ ] **0.1 Ратификация владельцем** пяти пунктов §10 канона истории:
  отмена «функции, не персонажи»; склейка «Анна = staff-Анна (04-02)»;
  жанр реплик `**Ты:**`; чат-жанр Зои строчными; открытые вопросы приняты
  к сведению.
- [ ] **0.2 Правка скилла `lecture-writer`** (`.claude/skills/lecture-writer/`):
  - `references/techniques.md` §9: заменить «персонажей нет (и не добавлять)»
    на «персонажи — по канону `docs/story-canon.md`»; добавить приём
    «сцена по канону истории» со ссылками на §4/§5/§9 канона;
  - `references/unit-anatomy.md`: слоты сцен (открытие/перекур/мостик/
    закрытие модуля) в каноническом скелете + правило отката + правило
    EN-зеркала «не вводить he/she о читателе»;
  - `references/checklist.md`: новые гейты сцен (см. раздел 7 ниже);
  - `SKILL.md`: story-canon.md в список обязательного чтения.
- [ ] **0.3 Линтер `check_unit.mjs`** — новые проверки:
  - RU/EN-инвариант: число строк `> **Имя:**` **именованных персонажей**
    совпадает в зеркалах (ошибка); реплики `**Ты:**`/`**You:**` не
    считаются, пока открыт вопрос §11.1 о форме EN-реплик читателя;
  - гендер-эвристика: слова на «-л/-ла» и краткие родовые формы в репликах
    `**Ты:**` и в ремарках сцен (предупреждение, ручное ревью); граница
    «сцены» для линтера — от первой строки `> **` до первого `##`-заголовка
    после блока диалога;
  - строка `> **Имя:**` внутри секции `## Запуск` — ошибка (реплики живут
    только в слотах §4.1 канона);
  - реплика длиннее ~40 слов — предупреждение «лекция в реплике»
    (прокси-гейт «владение концептом не мигрирует», §5.7);
  - отчёт длины сцены в словах против холодного открытия юнита
    (информационно, порог ×1.5 — ручная проверка по diff).
- [ ] **0.4 Перекрёстные правки канона подачи** (`course-canon.md`):
  - §2: абзац-ссылка «люди истории — в `story-canon.md`»;
  - §2, строка «разработчик / приложение»: переатрибуция по канону истории —
    «забытый WHERE (03-03)» → флешбэк Дани из времени до прихода читателя;
    добавить личные ставки читателя 02-06 (ALTER в обед) и 10-03 (N+1 в
    свежем PR); уточнить, к какому юниту относится «не тот индекс (06)»;
  - §2, строка «аналитик сети»: пометить, чьим голосом его запросы звучат в
    сценах (08-02/08-03 — Стас и Виктор; сам аналитик остаётся закадровой
    ролью);
  - в `20260611-pedagogy-uplift.md` — пометка о частичном supersede (раздел
    «Чего у Практикума брать НЕ надо», пункт про персонажей).
- [ ] **0.5 Гармонизация 00-01** (только RU — EN-зеркало уже нейтрально):
  строка «Привет. Ты только что вышел бэкенд-разработчиком в **Brew** —
  сеть кофеен с собственным приложением.» → «Привет. Ты — новый
  бэкенд-разработчик **Brew** — сети кофеен с собственным приложением.» —
  до раскатки гендер-правила реплик.
- [x] **0.6 Память проекта:** пометить в памяти
  (`memory/pedagogy-uplift-analysis.md`) вывод «добавлять функции, не
  персонажей» как superseded со ссылкой на `docs/story-canon.md` — память
  автоматически попадает в контекст будущих сессий, это тот же канал вето,
  что скилл и старый план. *(Выполнено 2026-07-03 при подготовке плана.)*

Фаза 0 не трогает ни одного демо, stdout или схемы — только документы,
скилл, линтер и память.

## 4. Фаза 1 — пилот (жанровый срез, 4 юнита + микро-формы)

Цель: проверить каждый жанр канона на реальных юнитах и откалибровать тон
до массовой раскатки.

| Юнит | Что пишем | Жанры под проверку |
|---|---|---|
| `00-01-meet-brew` | сцена знакомства (эталон §9.1 канона) + правка карты курса под персонажей | сцена-открытие курса, катехизис, setup'ы нитей |
| `00-06-connection-lifecycle-and-pooling` | war-room `FATAL: too many clients` | сцена-кульминация, Зоя в инциденте, мостик-реплика |
| `02-06-alter-table-migration-mindset` | личный инцидент читателя + первый постмортем, Зоя заводит блокнот | постмортем-жанр, чат-рапорт Анны, «виноват способ, не человек» |
| `04-01-joins-inner-left-right-full` | сцена Карины (эталон §9.2 канона) | сцена середины курса, катехизис-рефрен, правило отката (FULL JOIN-сверка сохраняется в теле) |
| `02-04-unique-and-check` | микро-реплика §9.3 канона (чат Паши 23:50 + рапорт Анны) | чат-жанр, **ввод Анны и Паши** — до их упоминаний в 02-06 |
| `00-02…00-05` | микро-формы: Зоя выдаёт песочницу, Даня про `$1` | лестница деградации, микро-диалоги |

Жанр «шапка Зои над Заборчиком» в пилоте не проверяется (в модуле 00 его
нет по карте) — первый носитель — `02-03` в волне I. Порядок внутри пилота:
`02-04` пишется **до** `02-06`, чтобы Анна была введена к своему рапорту
«Кассы. Обе. 12:41» (иначе 02-06 вводит её сам одной строкой прозы, а 02-04
при волне I ссылается назад).

Порядок работ на юнит: RU-сцена → EN-зеркало → `check_unit.mjs --against-head`
→ `make web-check-coverage` → `make web-build`. Секции `## Запуск`, код,
`query.sql` не тронуты по построению — линтер сторожит.

**Критерий выхода из пилота:** владелец подтверждает тон (кринж-тест сцен);
все гейты зелёные; замер длины уроков (cap ×1.5 соблюдён); EN-версии сцен
00-01 и 04-01 вносятся в §9 канона как EN-эталоны (без них волнам не с чем
сверять ритм EN-сцен); закрыт открытый вопрос §11.1 о форме `**You:**`.
После пилота — ретро: правки канона истории, если тон потребовал (канон —
живой документ, правки до раскатки дёшевы).

## 5. Фаза 2 — раскатка волнами по актам

Юнит = RU+EN вместе. Внутри волны — по номерам; полные сцены — только ★ из
§6 канона (поурочная карта — раздел 9). После каждой волны —
консистенси-чек: нити §7 (скриптовый греп по колонке «греп-маркер» реестра
§7, RU и EN), голоса против реестра маркеров §2, линтер по всем тронутым
юнитам. Чек волны обязан выдать список нитей в состоянии «setup написан,
payoff ещё нет» — это легальное состояние (см. риск «обрыв между волнами»
в разделе 8), но оно фиксируется явно.

| Волна | Модули | Акт | Особенности |
|---|---|---|---|
| I | 01, 02 (кроме 02-06), 03 | Акт I «Тебе объясняют» | первое появление Виктора (01-01) и Стаса (01-04); флешбэк Дани (03-03); расплата Стаса (03-06) |
| II | 04 (кроме 04-01), 05, 06 | Акт II «Вместе» | камео-склейка Анны (04-02, после ратификации §10.2); «неделя инцидентов» 05; рифма 02-06→06-06 |
| III | 07, 08 | Акт II | пик арки Дани (07-02); модуль 08 — пониженная плотность, правило отката для 08-01 |
| IV | 09, 10 | Акт III «Объясняешь ты» | ввод Тимура (09); спор менторов (09-05); переворот ролей и кольцо (10-05) |

Оценка объёма: ~20 полных сцен × 2 языка (150–250 слов сцена) ≈ 8–10 тыс.
слов нового текста; микро-формы в ~30–40 юнитах × 30–60 слов; каждая правка —
только README-проза.

## 6. Фаза 3 — сквозной проход и закрытие

- [ ] Проход по реестру нитей §7: каждый setup имеет payoff в тексте —
  скриптовый греп по колонке «греп-маркер» реестра (RU и EN) + ручная
  сверка нитей с маркером «—»; брошенных клиффхэнгеров нет (главная авария
  Практикума).
- [ ] Сверка голосов: выборочное чтение сцен подряд «глазами одного
  персонажа» — Зоя не заговорила длинно, Марат не пошутил, Даня не стал
  клоуном.
- [ ] Полный линтер по курсу + `make web-check-coverage` + `make web-build`.
- [ ] Обновить `story-canon.md`: снять статус «черновик», внести фактические
  отклонения (канон = источник правды для будущих юнитов).
- [ ] Обновить память проекта и `CLAUDE.md` (абзац про канон истории рядом со
  ссылкой на канон подачи).

## 7. Гейты юнита (свод)

Существующие (не меняются): `make db-reset` идемпотентен; `make gen` без
диффа; `make run` == `## Запуск` байт-в-байт; `make build`; RU+EN оба;
`make web-check-coverage`; `make web-build`; каллауты рендерятся.

Новые (сцены):

1. сцена **заменяет** прозаический пересказ боли; длина ≤ ×1.5 заменённого
   (по словам);
2. сцена сильнее заменяемого открытия — иначе форма ниже по лестнице (§4.3);
3. каждая сцена несёт типизированную функцию (§5.1) — «просто мило» не
   принимается;
4. реплики не пересказывают stdout и числа вывода;
5. реплики `**Ты:**` и ремарки — гендер-нейтральны (линт-предупреждения
   разобраны);
6. число реплик RU == EN (линт-ошибка);
7. forward — только прозой; каждый setup — в реестре §7;
8. юмор без каламбуров/эмодзи, переживает перевод;
9. владение концептом не мигрирует (§5.7): сцена подводит, объясняет
   юнит-владелец;
10. персонажи данных (Алиса/Борис/Карина, бариста, Дина/Егор) реплик не
    получают;
11. реплики — только в слотах §4.1 (открытие / перекур / мостик / закрытие
    модуля); внутри теории и `## Запуск` — запрещены (линт-ошибка);
12. ратификации §9 канона подачи — жёсткие входы: развод тёзок прозой
    (§9.1), дедуп называется «по `outbox_id` (таблица
    `processed_outbox_ids`)» (§9.4), `amount` заказа #1 — «итог-заголовок vs
    позиции», 10.50 и 9.70 рядом без объяснения не печатаются (§9.7).

## 8. Риски и митигации

| Риск | Митигация |
|---|---|
| Тон: «взрослый процедурал» скатывается в детсад или кринж | эталонные сцены §9 канона как камертон; один авторский проход на волну; writer/humanizer-пасс; пилот с явной приёмкой тона владельцем |
| Уроки распухают, дозировка ≤4–5 абзацев ломается | правило «сцена заменяет», cap ×1.5, правило отката; модуль 04 уже уплотнён Тиер-0 — там сцены только по правилу отката |
| Даня — громоотвод | балансировка §2.3 канона: часть инцидентов у читателя и «прошлого кода», Дане — победы |
| Голоса сливаются между волнами/агентами | реестр маркеров RU/EN в §2 канона; сверка голосов в Фазе 3; канон читается целиком перед каждой правкой |
| Рассинхрон RU/EN сцен | линт-инвариант числа реплик; RU+EN пишутся одним проходом |
| Выдыхание к финалу (авария Практикума) | бюджет-константа ~20 сцен; лестница деградации; переворот ролей и Тимур как топливо Акта III; setup'ы только с адресами |
| Конфликт со скиллом lecture-writer | Фаза 0.2 до любых правок прозы |
| Forward-валидация движка | реплики-обещания — только прозой; проверка в гейте |
| Гендер-нейтральность хрупка на объёме | линт-эвристика + правило «ремарки в настоящем времени»; EN не подвержен |
| Сцена затирает сильное открытие | правило отката §4.3 + notes поурочной карты («что обязано выжить») |
| Обрыв раскатки между волнами (setup написан, payoff — в будущей волне) | волны идут по хронологии курса, поэтому висящий setup читается как честное forward-обещание прозой — это легально; консистенси-чек волны выдаёт явный список «setup есть, payoff нет»; статус нитей помечается в §7 канона; при длительной паузе курс остаётся цельным — деградация по §4.2 |

## 9. Поурочная сюжетная карта

<!-- SCENE-MAP:BEGIN -->
Карта построена волной из 11 агентов по канону истории (формы — лестница §4.3;
полные сцены — только ★ из §6). Итог: 63/63 урока, из них **19 полных сцен**,
20 микро-диалогов, 21 микро-реплика, 1 плашка-сводка, 2 шапки Зои.
«Гейт-заметки» каждого урока — обязательный вход для переписчика: что в текущем
открытии обязано выжить и какие правила канона в зоне риска.

Терминология карты: «разметка» = по-урочный анализ текущего состояния
нарратива, собранный агентами первой волны исследования (промежуточный
артефакт сессии 2026-07-03; его выводы либо влиты в pitch/notes ниже, либо
помечены словом «предложение:» — такие пункты не канон, а кандидаты на
решение владельца). Счёт сцен: 19 юнитов со ★ = **20 сценовых слотов**
(10-05 — ★★, две сцены в одном юните), что соответствует бюджету §4.2.

### 00-getting-connected

> Бюджет: 2 полные сцены на модуль: 00-01 (сцена знакомства, образец §9.1) и 00-06 (war-room «too many clients») — ровно совпадает с двумя ★ в §6 канона истории для модуля 00. Остальные четыре урока — микро-формы (2 микро-диалога, 1 микро-реплика, 1 микро-диалог с предписанной §6 репликой Дани про $1), что соответствует указанию «микро-формы в 00-02…00-05». Перекуров два (00-05 и 00-06) — в норме «~1 на 2-3 урока» для шести уроков.


#### `00-01-meet-brew` — ★ Полная сцена

**Участники:** Марат, Даня, Зоя, Ты.

Первый рабочий день: Марат ведёт короткую экскурсию («одна база, девять таблиц»), новичок не верит, что за сетью кофеен стоит так мало. Даня сеет мем «просто» и пророчество «у нас так касса вставала» («Это было один раз. — Это было два раза»), Зоя из-за перегородки ставит границу («База — моя. Прод не проси»). Марат показывает на экране заказ #1 Алисы Ивановой (Виктор и чек в рамке — упомянуты, без реплик) и произносит катехизис: «покажи запрос», «что база ответила». Уход в тему: финальная реплика Марата — «начнём с вопроса попроще: что происходит на том конце сокета» — прямо открывает карту курса и перепись таблиц.
- **Нити §7:** Setup шести нитей §7: «Это было один раз/два раза» (payoff 02-06, 06-06), мем «просто» (02-02, 07-02, 09-03), outbox-тизер Дани («лучшая наша таблица» → 09-03), «маленькая доработка» Стаса (→ 01-04), чек заказа #1 (08, 10-05), катехизис Марата (рефрены 04-01/06-01, кольцо 10-05). Старт арок Дани и Зои.
- **Гейт-заметки:** ★ по §6 — форма канонная. Сцена уже написана целиком в §9.1 канона истории — брать как есть, не пересочинять. Обязано выжить: якорь заказа #1 (Алиса Иванова, 15 января 9:00, капучино + колд брю, статус paid), карта курса и обещание финала→kafka-cookbook, перечисление ролей (упоминание Стаса «маленькая доработка» — сеет его мем без личного появления). Предусловие §3.2: маскулинное «Ты только что вышел…» гармонизировать в настоящее время ДО вставки сцены. Сцена заменяет онбординг-прозу, а не добавляется (гейт 1.5× по словам). Существующий handoff в 00-02 сохраняется — он уже совпадает с финальной репликой Марата.


#### `00-02-client-server-and-sandbox` — Микро-диалог

**Участники:** Зоя, Ты.

Зоя выполняет обещание из сцены 00-01: выдаёт песочницу («docker compose. здесь ломай сколько хочешь. прод не проси»). Ты уточняешь про боевую базу — телеграфный отказ Зои маркирует границу «всё, что внутри сервера, — моё; ты — клиент с той стороны сокета» и подводит к теории клиент/сервер, которую объясняет сам юнит.
- **Нити §7:** Нитей §7 не задевает. Внутримодульная связка: payoff реплики Зои «Песочницу подниму к обеду» из образцовой сцены §9.1 (00-01) — микро-форма предписана §6 («Зоя выдаёт песочницу»).
- **Гейт-заметки:** Текущее открытие сухое, боли нет — микро-диалог дешёвый и чисто выигрывает (правило отката не мешает). Обязано выжить: якорь «локальная копия, которую не страшно сломать», превью конвейера sqlc, анонс psql в 00-03. Владение (§5.7): концепты клиент/сервер и критерий escape-hatch объясняет теория юнита — Зоя только ставит границу, не читает лекцию. 2-3 реплики максимум, дальше — существующая проза.


#### `00-03-psql-survival-kit` — Микро-диалог

**Участники:** Даня, Марат, Ты.

Даня перекидывает тикет: «колд брю пропал из меню на сайте» — ты рефлекторно открываешь код, Марат останавливает: «Что база ответила? Три команды — и узнаешь». Существующая развязка сохраняется: данные на месте, баг в коде — psql закрыл вопрос за десять секунд.
- **Нити §7:** Эхо катехизиса Марата (setup 00-01): реестр §7 называет рефрены в 04-01 и 06-01 — раннее эхо в 00-03 в реестр не внесено, помечаю как предложение (см. notes). Новых setup'ов нет.
- **Гейт-заметки:** Существующая боль (пропавший колд брю) сильная, но безличная — микро-диалог её персонифицирует, не заменяя расследование. Обязано выжить: факты развязки (CLD-01, stock=40, «баг не в данных, а в коде», «три команды и десять секунд») — остаются в прозе/выводе, реплики их НЕ пересказывают (§5.4); выбор Adminer vs терминал остаётся прозой без носителей мнений. Предложение: рефрен «Что база ответила?» здесь — самое естественное первое применение катехизиса после setup'а; если гейт строг к реестру — заменить реплику Марата на «Сначала спроси базу» без дословной формулы. Репортёром боли не ставить Анну (её первое появление — модуль 02, склейка — 04-02) и не Стаса (лично появляется с модуля 01).


#### `00-04-connecting-from-go` — Микро-диалог

**Участники:** Марат, Даня, Ты.

Ты сдаёшь первую фичу — поиск по категории — и на ревью Марат молча вбивает в поле «' OR 1=1 --»: витрина отдаёт всё меню. Твоё «но на демо же работало!» встречает нормализацию Дани: «У меня то же было в первый месяц. На тесте с десятью клиентами летало» — и его комментарий про `$1` (предписанная §6 микро-форма). Уход в тему: два конверта расширенного протокола и правило «$1 — не склейка».
- **Нити §7:** Нитей §7 напрямую не задевает; касается арки Дани (нормализация ошибки — его функция по §2.3), вех реестра не сдвигает. Правило «$1 не склейка» — посев модуля 00 по канону подачи §1.3, владелец — этот юнит.
- **Гейт-заметки:** Уязвимый запрос остаётся за читателем — атрибуция канона подачи §2 («разработчик = ты»), не переносить на Даню; это первая мягкая личная ставка, Марат не устраивает разнос (его манера — считать вместе). Обязано выжить: анти-демо инъекции в теле юнита, сравнение двух путей (склейка vs параметры), анонс 00-05 «ошибки ловит сборка, а не рантайм». Даня комментирует `$1`, но объясняет протокол юнит-владелец (§5.7). Реплика Дани — его голосовой маркер (цифры-оправдания, самоирония), переживает перевод.


#### `00-05-typed-queries-with-sqlc` — Микро-реплика

**Участники:** Даня, Марат, Ты.

Одна цитата Дани в чате внутри существующего открытия: переставил колонки в SELECT — тесты зелёные, а латте на витрине уехал в категорию tea; ручной Scan промолчал. Материализует гипотетическое «name молча ляжет в category» из текущей прозы и уводит в тему: этот класс багов обязан ловиться сборкой → sqlc.

- **Перекур:** После блока про конвейер генерации — микро-диалог 2-3 реплики, совместить с существующей секцией «чем sqlc не является»: Даня: «Так это же ORM, только сбоку?» — Марат: «Нет. SQL остаётся твоим. Генерируется только обвязка». Функция — встроенный FAQ (§5.1), готовая пара вопрос→ответ в конвенции каллаутов.
- **Нити §7:** Нитей §7 не задевает. Внутримодульная связка назад на 00-04 (тот же цикл Scan теперь генерируется) — сохраняется прозой.
- **Гейт-заметки:** Текущее открытие — техническая ретроспектива без бизнес-боли; полная сцена не положена (нет ★) и не нужна. Предложение: цитата Дани слегка материализует гипотетический инцидент из прозы в конкретный случай — это «раздача реплики написанной боли» (§1.4), но новых фактов мира не сеет; если гейт строг — деградировать до безличной формулировки текущей прозы (ступень 6 легальна). Обязано выжить: демо-факты (CLD-01, зелёный чай), связка «тот же Scan из 00-04», анонс 00-06 про пул как чёрный ящик. Инцидент Дани не превращать в постмортем — его первый настоящий постмортем-флешбэк живёт в 03-03.


#### `00-06-connection-lifecycle-and-pooling` — ★ Полная сцена

**Участники:** Зоя, Марат, Ты, Даня, Виктор (в чате).

Вечер запуска сайта Brew: трафик растёт, логи краснеют «FATAL: sorry, too many clients already» — первый war-room курса. Зоя смотрит pg_stat_activity с серверной стороны, ты — pool.Stat() с клиентской; виновник находится вместе — pgx.Connect на каждый HTTP-запрос в коде запуска. Разрешение — пул как ответ и короткий постмортем, из которого рождается таблица «Типичные ошибки модуля 00». Мостик: Виктор в чате просит первый отчёт по выручке → модуль 01.

- **Перекур:** После самого тяжёлого блока (жизненный цикл и цена соединения) — 2-3 реплики, по возможности совместить с существующей самопроверкой: Ты: «Сто соединений — это же просто сто сокетов?» — Зоя: «Сто процессов. Память. Планировщик.» — Марат: «Поэтому пул». Функция — аудит скрытого допущения (§5.1); объяснение цены остаётся в теории юнита-владельца.
- **Нити §7:** Setup нити «War-room пула» §7: 00-06 → 05-06 (advisory утёк) → 09-04 (NOTIFY и пул) → 10-04 (PgBouncer, дежурство читателя). Мостик к арке Виктора: его голос из чата здесь — преддверие первого личного появления в 01-01 («Где мои деньги?»). Двусторонний взгляд Зоя-сервер/ты-клиент — первый кирпич её арки к «Слот твой» (10-05).
- **Гейт-заметки:** ★ по §6 — форма канонная, кульминация модуля. Сцена заменяет безличное «расследование показало», а не техническое ядро. Обязано выжить: существующая таблица «Типичные ошибки модуля 00», двойной handoff (итог модуля + анонс numeric vs float), все числа pool.Stat/pg_stat_activity остаются в stdout — реплики на них смотрят, но не пересказывают (§5.4). Виктор — строго голосом из чата (дефицит §2.5, лично впервые в 01-01), реплика без единого технического слова. Виновника инцидента НЕ персонализировать: канон не отдаёт его ни Дане, ни читателю — оставить за «кодом, написанным до пула» (балансировка §2.3). Зоя не объясняет теорию пула — владелец концепта «пул и его цена» этот юнит; её реплики — граница и факты сервера.

### 01-data-types

> Бюджет: 1 полная сцена (01-01) — ровно столько ★ стоит в §6 канона истории для модуля 01 («Типы, первое появление Виктора»). Остальные уроки — микро-формы по лестнице §4.3: два микро-диалога (01-02 микро-детектив Карины, 01-04 первое личное появление Стаса — оба прямо предписаны §6), две микро-реплики (01-03 чат-пара, 01-05 цитата + чат-мостик), один перекур (01-03). Общий ориентир §4.2 «2–3 сцены на модуль» в модуле 01 сознательно не выбран — вторая ★ в §6 отсутствует; предложение о возможной второй сцене (01-04) зафиксировано в notes без изменения формы.


#### `01-01-numbers-and-money` — ★ Полная сцена

**Участники:** Виктор, Марат, Ты.

Утро после первого отчёта по выручке (его Виктор просил в чате в мостике 00-06): Виктор впервые лично поднимается в опенспейс с двумя распечатками — отчёт и касса расходятся на копейки в каждой строке: «Почему два отчёта — два числа? Где мои деньги?». Ты предполагаешь округление на витрине кассы; Марат-финтеховец молча берёт салфетку и пишет 0.1 + 0.2 → 0.30000000000000004 — рождается нить «салфетки Марата». Никто не крал копейки — их съело двоичное представление во float легаси-кода отчёта. Сцена обрывается требованием Виктора «чтобы копейки сходились», и теория юнита отвечает: деньги = BIGINT-центы.
- **Нити §7:** «Салфетки Марата» — setup (§7: 0.1+0.2 в 01-01 → 03-06, 06-02 → 10-05); «Деньги = BIGINT-центы под защитой» — setup (канон подачи §3: 01-01 → 02-04 CHECK → 10-02); арка Виктора — первое появление (§2.5); payoff мостика 00-06 «Виктор в чате просит первый отчёт».
- **Гейт-заметки:** Обязано выжить: конкретика боли «не сошёлся с кассой на копейки в каждой строке» (числа боли канонизированы — исключение §5.4). Сцена ЗАМЕНЯЕТ безличную прозу боли, а не добавляется (гейт 1.5× по словам, §5.2); объяснение float→numeric→BIGINT-центы остаётся в теле теории — юнит сам владелец концепта (канон подачи §5), Марат на салфетке только подводит. Катехизис «Покажи запрос» в сцену не тащить — бережём дефицит рефрена (эхо-точки перечислены в §7, модуля 01 среди них нет). Виновник float — легаси-код, не Даня и не читатель (балансировка §2.3, личные ставки §3.3). Арка Виктора сверена: 01-01 — его первое личное появление (строка арки §2.5 канона поправлена 2026-07-03). Заборчик про numeric/деньги можно открыть шапкой-Зои (ступень 5, опционально — по решению переписчика).


#### `01-02-text-boolean-and-null-teaser` — Микро-диалог

**Участники:** Стас (в чате), Марат, Ты.

Первый эпизод сериала Карины: Стас (пока только в чате) сверяет рассылку лояльности с отчётом «заказы по клиентам» — «Карина — не получила», хотя в базе она есть. Марат кивает на count(order_id): «посчитай ещё раз — count(*)»; расхождение 1 vs 0 вскрывает NULL, и вопрос «что такое NULL на самом деле» паркуется прозой до модуля 03.
- **Нити §7:** «Сериал Карины» — setup (§7: 01-02 «не получила рассылку» → 04-01, 04-05 → 08-05 «Опять Карина?!»); «Обучение Стаса» — setup (§7: первый NULL → 03-06 расплата → 10-02); «Ловушка NULL» — тизер (канон подачи §3: 01-02 → 03-06 → 04-05).
- **Гейт-заметки:** Правило отката §4.3: текущее открытие — лучшее в модуле (готовый Карина-детектив), микро-диалог достраивается вокруг него, каркас не заменяет. Обязаны выжить: Карина как героиня завязки и развязка «вот и Карина» в выводе, шпаргалка count(*) vs count(col), таблица LEFT JOIN с Алисой Ивановой и Борисом Петровым, двойной проговор арки NULL → 03-06. Карина — персонаж данных, реплик не получает (§2.9): Стас говорит О ней (её профиль на телефоне — его фирменный реквизит). Стас здесь только голосом из чата — первое личное появление зарезервировано §6 за 01-04. NULL не объяснять — владелец 03-06 (канон подачи §5): только тизер и парковка прозой. Не дублировать образцовую сцену 04-01 (§9.2): здесь виновник — count по NULL-колонке, там — INNER JOIN; реплика «мне не нужен джойн, мне нужна Карина» остаётся за 04-01.


#### `01-03-date-time-timestamptz` — Микро-реплика

**Участники:** Стас (в чате), Зоя (в чате, строчными), Даня (перекур), Марат (перекур).

Чат-пара по образцу §9.3 вместо безличного «покопались — нашли»: Стас (в чате) прикладывает две выгрузки одного дня с разным временем одного заказа — «какое из них настоящее?»; зоя (строчными): «настоящих ноль. колонка без зоны. база хранит то, что ей дали». Дальше — существующая проза расследования и теория «инстант vs настенные часы».

- **Перекур:** Да — самый мозголомный урок модуля. Даня: «в прошлом проекте я просто хранил всё в bigint-миллисекундах от эпохи — никаких зон, никаких проблем» — Марат двумя вопросами возвращает зоны обратно («а во сколько открывается кофейня? а „утро“ акции — чьё утро?»). Совместить с существующей самопроверкой юнита (вопрос — [!NOTE], ответ — [!TIP]); функции: нормализация ошибки + аудит скрытых допущений.
- **Нити §7:** «Переезд во второй город / время» — setup (канон подачи §3: 01-03 → 02-02, вторая точка получает код-ключ shops); новых нитей §7 юнит не заводит; слово «просто» у Дани в перекуре — рефрен мема §7 (setup 00-01), не новый setup.
- **Гейт-заметки:** Обязаны выжить: числа боли (12:00 по Москве vs 09:00 в UTC-аналитике), три SET TIME ZONE на одном значении, якорь «момент времени vs настенные часы», заказ #1 как данные. Конфликт хронологии: проза юнита утверждает «Brew открылся во втором городе», а мостик модуля по §6 — Стас только ПРИНОСИТ план второй точки в конце 01-05 (и 02-01 открывается переливкой истории для новой точки); предложение: переформулировать боль 01-03 под пилотную/выездную точку или выгрузку для партнёра в другом поясе, чтобы постоянная вторая точка оставалась будущим до модуля 02. Владелец timestamptz — сам юнит, конфликтов владения нет.


#### `01-04-uuid-and-uuidv7` — Микро-диалог

**Участники:** Стас, Ты, Зоя (в чате + шапка над Заборчиком).

Стас впервые спускается лично — со словами «маленькая доработка» (пророчество Марата из 00-01 сбывается): ссылки-приглашения для регистраций лояльности; через неделю конкурент публикует точное число регистраций Brew — /signup/42 перебирается. Второй акт приносит Зоя в чате (график вставок в loyalty_signups просел после переезда на v4, сортировка «последних» умерла); развязка uuidv7 — в теории, а Заборчик byte-compat открывается репликой Зои: «customers.id не трогаем. Контракт с соседней командой».
- **Нити §7:** Byte-compat впервые звучит репликой (прямое требование §6; маршрут «CDC-эстафета в kafka-cookbook» канона подачи §3 → 09-03 → 10-05, где правило произносишь уже ты); арка Стаса — первое личное появление, payoff интро Марата из 00-01 («он появляется со словами „маленькая доработка“, и это ещё ни разу не было правдой»).
- **Гейт-заметки:** Обязана выжить двухактная драматургия текущего открытия (первое «решение» v4 → второй удар эффектнее первого: сортировка + фрагментация B-tree) и конкретика /signup/42. Владение: выбор типа ключа — этот юнит; механика ALWAYS vs BY DEFAULT — у 02-01, здесь не преподавать (канон подачи §5). Реплика Зои над Заборчиком — приём «шапка-зои» (ступень 5) поверх существующего авторского заборчика byte-compat; её чат-рапорт — строчными, назывными (§4.4). Ремарки о доработке — в настоящем времени второго лица (гендер-линт §3.2). Предложение: если владелец поднимет бюджет модуля до 2 сцен (§4.2 допускает 2–3 на модуль), кандидат на вторую ★ — именно 01-04 (первое личное появление Стаса + готовая двухактная структура); по текущему §6 — микро-диалог.


#### `01-05-enums-arrays-and-jsonb-intro` — Микро-реплика

**Участники:** Стас.

Открытие остаётся прозой-планированием (единственный урок модуля без пожара), но три бизнес-запроса становятся одной цитатой Стаса в прозе: размеры S/M/L, теги для блога и «гости просят сироп, корицу, „без крышки“ — куда это класть?» — триаж enum/массив/jsonb делает теория. Закрытие модуля — мостик §6: последняя чат-реплика Стаса — план второй точки согласован → модуль 02.
- **Нити §7:** jsonb-intro → модуль 07 (канон подачи §1.3: модуль 07 «платит» jsonb-intro из 01-05; GIN/containment — 06-05 → 07); мостик модуля — «Стас приносит план второй точки → 02» (§6), рифмуется с setup'ом «Переезд во второй город» из 01-03.
- **Гейт-заметки:** Полной сцены не заслуживает и по канону не получает — деградация до микро-реплики легальна и правильна. Обязаны выжить: три бизнес-запроса как каркас урока, тройной проговор арки jsonb → модуль 07, заборчик «jsonb — самый опасный» (можно открыть шапкой-Зои — опционально, ступень 5). Формулу «поле для всего» и вердикт «Сорок nullable-колонок. Можно. Но больно» НЕ использовать — зарезервированы за ★ 07-01 (§6); просьбу Стаса формулировать иначе. GIN здесь — только intro оператора @>: владелец механики — 06-05 (канон подачи §5). В мостике не называть вторую точку NEVA — имя появляется только в 02-02 как ребрендинг BREW-NORTH; безымянный Даня-«маркетинг» текущей прозы становится Стасом (персонификация без новых имён, реестр §2.9).

### 02-schema-and-constraints

> Бюджет: Полных сцен — ровно 1 (02-06), что совпадает с единственной ★ в §6 канона истории для модуля 02 («★ Кульминация 02-06 — личный инцидент читателя»). Остальные пять уроков — микро-формы ступеней 2–5 (микро-диалог 02-02, микро-реплики 02-01/02-04/02-05, шапка-зои 02-03); перекур один на модуль (02-05), в норме «~1 на 2–3 урока». Бюджет §4.2 допускает 2–3 сцены на модуль — резервный кандидат на вторую ★ (если владелец решит расширить) отмечен в notes 02-02.


#### `02-01-identity-and-defaults` — Микро-реплика

**Участники:** Зоя (в чате).

Утренний рапорт Зои в чате (строчными, без точек): «orders_pkey. duplicate key. с шести утра сыплется.» — внутри существующего прозаического открытия про переливку истории; след ведёт к скрипту с явными id 1001..1500 и несдвинутой последовательностью. Разрешение — в теме юнита: кто владеет ключом, ALWAYS vs BY DEFAULT.
- **Нити §7:** Новых нитей §7 нет. Прозой — связка с 01-04 (модуль 02 «платит» рассинхроном serial за вопрос «кто владеет id»; владелец выбора типа ключа — 01-04, не переобъяснять). SQLSTATE-словарь модуля (428C9/23505) — канон подачи, живёт в stdout, не в репликах.
- **Гейт-заметки:** Обязано выжить: числа боли (id 1001..1500, «несколько недель работало», orders_pkey), механика setval в schema/seed.sql, вывод 428C9/23505 в демо. Зоя только рапортует — теорию не объясняет (граница §2.2); её чат-жанр строчными — фирменный маркер §4.4. SQLSTATE в чат-рапорте — из прод-логов, не пересказ stdout юнита (§5.4 соблюдён). Владение механикой ALWAYS vs BY DEFAULT — у этого юнита (§5 канона подачи).


#### `02-02-not-null-pk-natural-vs-surrogate` — Микро-диалог

**Участники:** Стас, Ты, Марат.

Стас спускается с телефоном экраном к тебе: ребрендинг, вторая точка BREW-NORTH становится BREW-NEVA — «это же один UPDATE?». Ты почти соглашаешься (гипотеза новичка, без экспертизы), Марат отвечает вопросом: «Что ссылается на этот код?» — и проза вскрывает каскад FK и кэши, ссылающиеся в пустоту. Уходит в тему: PK = NOT NULL + UNIQUE, изменчивый бизнес-код нельзя делать идентичностью строки.
- **Нити §7:** Реестр §7: мем «просто» — развитие (00-01 setup → 02-02 «это же один UPDATE?» → 07-02 → 09-03), реплика Стаса обязана прозвучать близко к канонной формулировке. Плюс существующая нить канона подачи §3 «переезд во второй город» (01-03 → 02-02) и мостик модуля 01 «Стас приносит план второй точки» — микро-диалог их принимает.
- **Гейт-заметки:** Обязано выжить: формула боли «изменчивый бизнес-код сделали идентичностью строки», коды BREW-CENTRAL/NORTH/NEVA (канонные значения shops), «отчёты держали старый код в кэше». Микро-диалог заменяет первую половину абзаца пересказа, гейт 1.5× по словам (§5.2). ★ в §6 здесь нет — полной сцены не ставим; предложение: если владелец захочет вторую сцену в модуле (бюджет §4.2 допускает 2–3), 02-02 — первый кандидат как сюжетное открытие модуля с мостиком из 01.


#### `02-03-foreign-keys` — Шапка Зои над Заборчиком

**Участники:** Зоя.

Открытие остаётся текущей прозой — оно сильное: «удалите мой аккаунт» → DELETE клиента id=42 → три точки поломки. История добавляется только над Заборчиком: «Зоя — в ревью, одной строкой: Каскад однажды снёс мне полбазы. Дефолт — RESTRICT.»
- **Нити §7:** Новых нитей §7 нет; байка Зои — её канонная предыстория из §2.2 («базы, которые однажды каскад снёс наполовину»), не новая нить. Норма «одна военная история Зои в модуль» расходуется здесь — в сцене 02-06 ей байка не положена.
- **Гейт-заметки:** Правило отката §4.3: открытие конкретное и сильное (висящие заказы, падающие отзывы, запрет удаления напитка, 23503) — не заменять сценой. Клиент id=42 остаётся безымянным: канонный каст — id 1–3, персонажи данных реплик не получают (§2.9); предложение из разметки «сделать его Борисом Петровым» отклонить. Виновник-разработчик остаётся «прошлым кодом» — не вешать на Даню (балансировка §2.3) и не на читателя (его личный инцидент модуля — 02-06). Обычная капитализация в реплике: lowercase — только чат-жанр (§10.4).


#### `02-04-unique-and-check` — Микро-реплика

**Участники:** Паша (в чате, 23:50), Анна (в чате, 08:12).

Канонический образец §9.3 применяется почти дословно: Паша в 23:50 — «прайс загрузил, всё зелёное. файл как файл.», Анна в 08:12 — «Пробиваю раф — ноль рублей. Так и надо?». База разрешила мусору доехать до кассы — юнит о том, как заставить её отказывать (UNIQUE, CHECK, NULLS NOT DISTINCT).
- **Нити §7:** Нить канона подачи §3 «деньги = BIGINT-центы под защитой»: 01-01 → 02-04 (CHECK > 0) → 10-02 — задевается прозой. Первое появление Паши и Анны в курсе: ввести обе роли одной строкой прозы (закупщик; управляющая первой кофейни) — правило ввода §2; склейка Анны со staff-Анной легализуется только в 04-02 (§10.2), здесь её не упоминать.
- **Гейт-заметки:** Держаться образца §9.3 — это эталон тона для ступени 3. Обязано выжить: числа боли (цена 0, размер XXL, дубли позиций, «касса отдавала бесплатно», импорт «успешно»), тизер-ссылка на 03-06 как владельца трёхзначной логики NULL (§5.7 — здесь только следствие для UNIQUE). Кейс NULLS NOT DISTINCT «ровно одна активная промо» можно привязать к Стасу прозой (без реплики — диалог в тело теории не ставим, §5.3).


#### `02-05-generated-columns-and-domains` — Микро-реплика

**Участники:** Стас, Даня, Марат.

Открытие: Стас с телефоном экраном к тебе — скрин от гостя: «В письме-чеке одна сумма, в корзине — другая. Гость спрашивает, какая настоящая.» Проза вскрывает три копии формулы qty×unit_price и пять копий CHECK — юнит убирает обе копипасты в схему (генерируемый столбец + домен).

- **Перекур:** После тяжёлого блока STORED vs VIRTUAL: Даня — «VIRTUAL же бесплатный. Делаем всё VIRTUAL?», Марат — «Бесплатный при записи. Кто платит при чтении?» (функция: встроенный FAQ + сократика). Существующей самопроверки в юните нет — перекур ставится самостоятельной парой реплик (или конвертируется в новую пару вопрос→ответ в конвенции каллаутов).
- **Нити §7:** Прозой — продолжение нити «деньги под защитой» (домен positive_cents наследует CHECK > 0 из 02-04, маршрут 01-01 → 02-04 → 10-02); рифма 428C9 с 02-01 уже есть в тексте — сохранить. Новых нитей §7 не заводим.
- **Гейт-заметки:** Обязано выжить: якоря боли (три места — корзина, письмо-чек, ночной отчёт; пять таблиц с копией CHECK), README-fence о причине escape-hatch (sqlc v1.30.0 падает на VIRTUAL — «выбираем фичу, не инструмент»). Предложение из разметки «Виктор с „где мои деньги“» отклонить: Виктор дефицитный (§2.5, 2–3 появления за курс), а «Где мои деньги?» — его якорь 01-01, повтор разбавит; ночной отчёт о выручке упомянуть прозой без его реплики. Расползшаяся формула остаётся «прошлым кодом» без атрибуции — Даня не громоотвод (§2.3).


#### `02-06-alter-table-migration-mindset` — ★ Полная сцена

**Участники:** Ты, Анна (в чате), Зоя, Марат, Даня.

★ Кульминация модуля и первый личный инцидент читателя: ты катишь «безобидную» миграцию ALTER COLUMN TYPE в обед — в чате Анна: «Кассы. Обе. 12:41.»; восемь минут ACCESS EXCLUSIVE, Зоя убивает запрос и молча заводит бумажный блокнот инцидентов. Первый постмортем: Марат произносит правило «виноват способ, не человек» и переводит разбор в тему юнита: мгновенно против переписывания, relfilenode, NOT VALID → VALIDATE. Мостик-концовка (последняя реплика = крючок, §4.1): Даня, глядя в постмортем: «Самый дорогой инцидент — вообще одна строка. UPDATE без WHERE. Знаю. Я автор.» — прозой в модуль 03.
- **Нити §7:** Реестр §7: «Это было один раз — это было два раза» — развитие (касса встаёт при читателе; payoff «Теперь — три» в 06-06); «Блокнот Зои» — SETUP здесь (заведён на первом постмортеме, payoff 10-05); арка Дани — его признание = setup флешбэка 03-03. Нить канона подачи §3 «опасная миграция на горячей таблице» 02-06 → 06-06 (CONCURRENTLY). Личный инцидент закреплён за читателем по §3.3 — не переатрибутировать.
- **Гейт-заметки:** Сцена заменяет прозаический пересказ первого абзаца (гейт 1.5× по словам, §5.2), но числа боли канонизированы и обязаны выжить: 8 минут, ACCESS EXCLUSIVE, «в разгар дня/в обед», контраст с мгновенным ADD COLUMN DEFAULT. «Кассы. Обе. 12:41.» — дословный маркер Анны (§2.6), в 06-06 повторится дословно — зафиксировать формулировку сейчас. Реплики «Ты» — 1–3, гендер-нейтральные (§3.2: без прошедшего времени первого лица). relfilenode-числа и вывод демо остаются в «## Запуск» — реплики их не пересказывают (§5.4). Существующий WARNING-каллаут про очередь блокировок и раздел NOT VALID → VALIDATE не трогать. RU/EN: число реплик в зеркалах обязано совпасть (§5.10) — оба README юнита сейчас в правке (git modified), сверить после.

### 03-crud-fluency

> Бюджет: 2 полные сцены — 03-03 (флешбэк-постмортем Дани) и 03-06 (финал-детектив NULL); ровно совпадает с ★ в §6 канона истории для модуля 03 и укладывается в бюджет §4.2 (2–3 сцены на модуль). Остальные четыре урока — микро-формы: микро-диалог на входе модуля (03-01), микро-реплики (03-02 Стас, 03-04 Паша-чат, 03-05 Стас). Перекуров два на шесть уроков (03-02, 03-06), оба совмещены с существующими самопроверками.


#### `03-01-insert-and-returning` — Микро-диалог

**Участники:** Стас, Ты, Марат.

Короткое открытие модуля (3 реплики): Стас спускается с запуском лояльности — «первую карту выдаём Алисе Ивановой, и номер сразу на экран». Ты предлагаешь очевидное «вставить, потом прочитать id» — Марат одним вопросом («а что происходит между твоим INSERT и твоим SELECT?») вскрывает гонку и лишний round-trip; проза уходит в RETURNING как идиому.
- **Гейт-заметки:** ★ по канону здесь нет — форма выше микро-реплики оправдана только тем, что это вход модуля; полной сценой не раздувать. Обязано выжить: анти-паттерн «два запроса» с гонкой между ними, карта BREW-0001 в lab-таблице loyalty_cards, форвард-ссылки на 09-01 (COPY) и 03-05 прозой. Требование Стаса «номер сразу на экран» канонизировано в §6 — это его реплика, не пересказ. Алиса Иванова — персонаж данных, реплик не получает (§2.9): о ней говорят. Реплики «Ты» — вопрос/гипотеза без рода (§3.2). Номер карты из stdout в реплики не тащить (§5.4).


#### `03-02-select-where-order-limit` — Микро-реплика

**Участники:** Стас, Даня, Марат.

Одна цитата Стаса внутри существующего прозаического открытия: «Гость долистал меню до пятисотой страницы — и приложение задумалось. Почему низ меню медленнее верха, кофе-то тот же?» Проза дальше как сейчас: OFFSET читает и выбрасывает, keyset листает от якоря.

- **Перекур:** После тяжёлого блока про keyset и кортежное сравнение — микро-диалог, совмещённый с существующей самопроверкой: Даня признаётся «я всегда беру LIMIT/OFFSET и кэширую» — Марат: «Посчитай, сколько строк база прочитала ради страницы пятьсот». Ответ — существующий каллаут-разбор.
- **Гейт-заметки:** Текущее открытие крепкое (боль «страница 500 / OFFSET 10000» уже есть) — по правилу отката §4.3 сцену не ставим, только голос заказчика поверх. Обязано выжить: сравнение OFFSET/keyset таблицей, числа боли (страница 500), форвард на модуль 06 (EXPLAIN докажет планом — владелец там, §5). В перекуре не использовать слово «просто» у Дани — реестр §7 фиксирует точки мема, новые не добавляем. Крючок-концовка в 03-03 остаётся прозой.


#### `03-03-update-delete-safely` — ★ Полная сцена

**Участники:** Даня, Марат, Ты.

★ Флешбэк-постмортем: Даня сам рассказывает новичку свой легендарный пятничный UPDATE без WHERE — все заказы Brew разом «отменены», паника, откат из бэкапа; инцидент случился до твоего прихода. Марат стил-мэнит («запрос был синтаксически безупречен — в этом и ужас»), ты задаёшь наивный вопрос «как увидеть масштаб до коммита?» — и сцена подводит к дисциплине BEGIN → проверка RowsAffected/RETURNING → ROLLBACK. Даня закрывает самоиронией — ошибка нормализована, страх конвертирован в правило.
- **Нити §7:** Арка Дани (§7): развитие — 00-01 «просто» → 03-03 флешбэк-постмортем → 07-02 → 10-01; пейофф мостика из финала модуля 02 («самый дорогой инцидент — одна строка. UPDATE без WHERE. Знаю. Я автор»).
- **Гейт-заметки:** Атрибуция по канону: инцидент до прихода читателя — «разработчик» из канона подачи не нарушается, читатель не громоотвод и не безошибочный наблюдатель (§3.3). Обязано выжить: формула боли «одна строка — все заказы cancelled», нарисованный таймлайн BEGIN→проверка→ROLLBACK (сцена его не пересказывает — теория владеет), RowsAffected, заборчик (ревью миграций, staging, мягкое удаление). Риск §5.2: текущая проза боли — одна строка, гейт 1.5× считаем от всего заменяемого холодного открытия и держим сцену компактной (6–8 реплик). Форвард на модуль 05 (MVCC/блокировки) — прозой. Блокнот Зои в сцену не тащить: заведён в 02-06, флешбэк-инцидент случился раньше (континуити §7). Опционально (решение переписчика): шапка-зои над заборчиком — «Зоя — в ревью, одной строкой».


#### `03-04-upsert-on-conflict` — Микро-реплика

**Участники:** Паша.

Чат-открытие в фирменном жанре Паши: «**Паша (в чате, 23:50):** ночной синк остатков упал. duplicate key. **Паша (в чате, 23:57):** перезапустил — прошло. файл как файл, что не так-то?» Проза подхватывает: «перезапустил — прошло» — худший вид бага, между SELECT и INSERT вторая точка прислала ту же пару.
- **Гейт-заметки:** Обязано выжить: SKU ESP-01/LAT-01 и точки CENTRAL/NORTH, SQLSTATE 23505 (контракт модуля 02), связка ON CONFLICT ↔ processed_outbox_ids (мост к Kafka-курсу, канон подачи §1.3), приём xmax<>0 — только как приём со ссылкой на 05-02 (владелец MVCC, §5.7 — Паша гонку не объясняет, объясняет теория). Крючок в 03-05 (RETURNING old/new) остаётся прозой. Форвард на 09-01 (COPY+MERGE, тот же Паша с файлом остатков) — прозой, это его функция по §2.7, но новой нити в реестр §7 не заводим.


#### `03-05-returning-old-new` — Микро-реплика

**Участники:** Стас.

Одна цитата Стаса в прозе открытия: «Пуш „статус заказа изменился“ уходит. На что изменился — гость видит. С чего — не видит никто, а спор „я платил, а вы отменили“ разбирать нечем. Верни мне „было“.» Дальше проза как сейчас: SELECT-до-UPDATE — гонка, триггер — рано, PG18 отдаёт old и new одной командой.
- **Гейт-заметки:** Обязано выжить: escape-hatch fence (sqlc v1.30.0 не парсит old.*/new.* — «выбираем возможность, а не инструмент»; остаётся прозой в fence, в реплики не выносить), гонка SELECT-до-UPDATE, lab order_status_lab с заказами #1–#4, форвард на 09-05 (триггерный аудит) прозой. Карину в адресаты пуша не ставить — её сериал зафиксирован реестром §7 (01-02 → 04-01 → 04-05 → 08-05), точек не добавляем. Не сталкивать с 09-02 (двойной пуш Алисе) — здесь боль про «было/стало», не про дубли. Крючок-интрига в 03-06 («расплата за тизер 01-02») — сохранить дословно, он уже работает.


#### `03-06-null-semantics-reckoning` — ★ Полная сцена

**Участники:** Анна, Ты, Марат, Стас, Даня.

★ Финал-детектив модуля. Триггер — рапорт Анны в чате («Приложение. Меню пустое. 09:20. Гости заказывают у стойки.»); запрос «напитки не в стоп-листе» вернул ноль строк — и ноль ошибок. Ты с Маратом идёте по следу («Покажи запрос» — запрос честный; «Что база ответила?» — база ответила «не знаю»), след приводит к вчерашней ручной правке стоп-листа Стасом — один NULL, и NOT IN молча съел всё меню; Марат на салфетке показывает лишь искру «true AND NULL = NULL». Стас получает расплату за тизер 01-02 и последней репликой бросает крючок в модуль 04: «NULL я запомнил. Теперь скажи — сколько всего гостей, как Карина: зарегистрировались и молчат?»

- **Перекур:** После самого тяжёлого блока (таблица истинности трёхзначной логики) — микро-диалог, совмещённый с существующей самопроверкой: Даня: «Так NULL = NULL — это же true, они одинаковые» — Марат: «NULL — не значение, а „не знаю“. „Не знаю“ равно „не знаю“?» Ответ — существующий каллаут.
- **Нити §7:** Ловушка NULL: расплата за тизер 01-02, callback дальше в 04-05 (§7 канона подачи и §7 истории — «обучение Стаса»: 01-02 → 03-06 → 04-04 → 10-02); салфетки Марата: развитие (01-01 → 03-06 NULL-логика → 06-02 → 10-05); катехизис Марата — рефрен «Покажи запрос / Что база ответила?»; мостик про Карину канонизирован в §6, следующая точка её сериала — 04-01.
- **Гейт-заметки:** Правило отката §4.3 — главное здесь: текущее открытие («вернул НОЛЬ напитков — без единой ошибки») лучшее в модуле; сцена строится ВОКРУГ этого ядра и обязана его сохранить, а не заменить абстрактным диалогом. Владение концептом: 03-06 — сам владелец трёхзначной логики (§5 канона подачи), поэтому салфетка Марата в сцене — только подводка «true AND NULL», полная таблица истинности и COALESCE/NULLIF/IS DISTINCT FROM — в теории юнита. Довод «NOT IN → NOT EXISTS» не переобъяснять — callback-владение у 04-05. Обязано выжить: безошибочность как ужас (логи чистые), связка-расплата с 01-02, арочная концовка модуля в 04. Рапорт Анны — телеграф с числами (§2.6); Стас не клоун — правка была бизнесово осмысленной, виноват способ, не человек. Реплики «Ты» — 1–3, вопросы без рода.

### 04-querying-across-tables

> Бюджет: В модуле 2 полные сцены — 04-01 (открытие, готовый образец §9.2) и 04-06 (закрытие-ретро) — ровно совпадает с ★ в §6 канона истории («★ 04-01 (образец §9.2)» и «★ Финал — ретро модуля у доски»). Бит Виктора «+40%» в 04-03 в каноне звезды не имеет — оформлен микро-диалогом; биты Стаса в 04-04/04-05 — микро-формами. Перекуров два на шесть уроков (04-03 и канонический §9.5 в 04-06, совмещённый с самопроверкой) — в квоте ~1 на 2–3 урока.


#### `04-01-joins-inner-left-right-full` — ★ Полная сцена

**Участники:** Стас, Марат, Ты.

Стас стоит у твоего стола с профилем Карины на телефоне: рассылка «Мы скучаем» ушла, а Карина — идеальный адресат — письма не получила, и хуже того, никто не знает, сколько ещё таких список потерял. Марат подходит с кружкой: «Покажи запрос» — и выносит вердикт: INNER её не потерял, а «выбросил. По правилам». Ты задаёшь вопрос новичка («а как спросить всех — даже тех, у кого пары нет?»), Марат обещает ответ одним словом, Стас закрывает сцену дедлайном «к обеду» и цифрой охвата для Виктора. Сцена уходит в тему юнита: четыре вида JOIN — кто кого сохраняет и кто кого выбрасывает.
- **Нити §7:** «Сериал Карины» (setup 01-02 → развитие здесь: INNER выбросил → payoff 08-05 «Опять Карина?!»); «Катехизис Марата» — канонный рефрен «Покажи запрос» закреплён реестром §7 именно за 04-01; «Обучение Стаса» (между 03-06 и 04-04). Мостик в 04-02 остаётся прозой.
- **Гейт-заметки:** Сцена уже написана в каноне (§9.2) — брать её как эталон, не пересочинять; она ЗАМЕНЯЕТ два первых абзаца прозы (гейт §5.2: не длиннее 1.5× по словам). Обязано выжить: формула «В цифрах отчёт не наврал. Он наврал умолчанием» — сохраняется дословно прозой-связкой после сцены (реплика Марата «Он её выбросил. По правилам» — не замена, а рифма); WHERE-ловушка «Карина снова исчезла» — остаётся в теле теории с перекличкой с холодным стартом; анти-join приём; FULL JOIN-сверка двух источников — остаётся прозой в теле (§9.2 прямо: «хук сверки FULL JOIN остаётся в теле юнита»). Отклонить предложения разметки: вторая сцена на WHERE-ловушку (бюджет — одна сцена) и реплики бариста/кладовщика в сверке (персонажи данных, §2.9). NULL-механика — владелец 03-06 (§5 канона подачи), сцена её не трогает.


#### `04-02-multi-table-and-self-joins` — Микро-реплика

**Участники:** Анна (в чате).

Чат-рапорт Анны приносит боль поддержки в её фирменном телеграфе: гость у стойки спрашивает состав своего заказа, а в админке видна только сырая строка orders с customer_id — нужен чек, собранный из четырёх таблиц. Дальше проза как сейчас: чек заказа #1 одним запросом, затем self-join по staff, где одной строкой прозы легализуется склейка «Анна-менеджер из таблицы staff — это она и есть».
- **Нити §7:** Ратификация §10.2 — камео Анны легализует склейку со staff-Анной (канонное место — именно 04-02); нить канона подачи §3 «обещание рекурсивного CTE» (04-02/04-06 → 08-04) — сохраняется прозой; развод тёзок staff-Борис ≠ клиент Борис Петров по §9.1 канона подачи — уже есть в тексте, сохранить.
- **Гейт-заметки:** Обязано выжить: якорь «чек заказа #1 из четырёх таблиц» (канонный локальный хук §3), введение термина fan-out с явным обещанием ловушки в 04-03, NOTE о lab-статусе staff, авторская связка про двух Борисов. Имена в демо/stdout не трогаем — склейка Анны живёт только в прозе. Анна — единственное исключение из §2.9 (камео-склейка); Вера, Глеб, staff-Борис и гость остаются персонажами данных без реплик. Отклонить предложения разметки: «оператор поддержки» и HR-сцена — новые персонажи канону не нужны, операционную боль несёт Анна.


#### `04-03-aggregation-group-by-having` — Микро-диалог

**Участники:** Виктор (в чате), Марат, Ты.

Виктор голосом из чата радуется новому отчёту: «+40% выручки за месяц — хорошая новость?» Марат не верит слишком хорошему числу, смотрит запрос (ремаркой прозой) и выносит вердикт: «Размножились слагаемые, а не деньги» — JOIN с order_items раздул строки заказов под sum(). Микро-диалог уводит в тему: агрегат поверх fan-out и count(*) vs count(колонка).

- **Перекур:** После тяжёлого блока «Агрегат поверх fan-out задваивает суммы»: Марат — «Что ответишь Виктору?», Ты — гипотеза («выросли не деньги, а слагаемые?»), Марат закрывает («И что отчёт теперь считает деньги до джойна»). Функция: передышка + закрытие петли открытия; 3 реплики.
- **Нити §7:** Бит модуля §6 (Виктор + fan-out, без звезды); «Сериал Карины» — развитие по канону подачи §3 (01-02 → 04-* → 08-05): count(*)=1 vs count(o.id)=0 держится в теории; дефицит Виктора §2.5 — только чат, личные появления зарезервированы за 01-01/08/10-05.
- **Гейт-заметки:** Обязано выжить: таблица четырёх агрегатов на строке Карины (count(*)=1 / count(o.id)=0 / sum→NULL / avg→NULL), приём COALESCE, TIP про count(DISTINCT) и FILTER, HAVING-блок. Число боли — канонные «+40%» из §6, вердикт дословно «размножились слагаемые, а не деньги». Рефрен «Покажи запрос» в этом модуле закреплён реестром §7 за 04-01 — здесь Марат говорит вердиктом, просмотр запроса оформить ремаркой. Реплики не пересказывают stdout (§5.4): числа Карины остаются в теории и выводе. Отклонить предложение разметки вернуть Стаса с «Карина с одним заказом» — второй голос перегрузит открытие, кейс Карины и так живёт в теле.


#### `04-04-distinct-on` — Микро-реплика

**Участники:** Стас.

Стас приносит заказ на карточку клиента и впервые сам формулирует требование правильно: «Мне нужна строка, а не число — max(даты) уже проходили». Одна цитата встраивается в существующий первый абзац; дальше проза юнита без изменений.
- **Нити §7:** «Обучение Стаса» — реестр §7 закрепляет за 04-04 ровно эту реплику («строка, не число»; развитие между 03-06 и 10-02); нить канона подачи §3 «DISTINCT ON 04-04 → LATERAL 08-05» — остаётся прозой.
- **Гейт-заметки:** Самый безлюдный вход модуля, но звезды в §6 нет — по канону остаётся микро-формой (ступень 3). Обязано выжить: tie-break-ловушка (при равных created_at «первая» строка не определена, отчёт плавает между прогонами; страховка id DESC), отсылка к 03-02 как владельцу идеи tie-break, честная пометка «DISTINCT ON — не стандарт SQL», ASCII-схема на Алисе/Борисе (персонажи данных, без реплик). Симптом плавающего отчёта («в карточке Алисы то #1, то #3») держать прозой в теле — вторая цитата нарушила бы слоты §4.1/§5.3. Отклонить предложения разметки: «оператор CRM» и диалог «а если переедем с Postgres» — новых персонажей нет, таблица альтернатив мотивируется прозой.


#### `04-05-subqueries-exists-vs-in` — Микро-диалог

**Участники:** Стас, Марат.

Стас завёл акцию «на всё меню» — и отчёт «напитки не в акции» опустел: ноль строк при трёх действующих акциях. Марат сократикой («Что лежит в подзапросе, когда акция — „на всё“?») подводит к NULL в наборе, и Стас узнаёт злодея сам: «Только не говори, что это опять NULL» — выученный урок 03-06. Проза юнита, как и сейчас, коротко напоминает механику и делает вывод: для «нет среди» — NOT EXISTS.
- **Нити §7:** «Ловушка NULL» (01-02 тизер → 03-06 расплата → 04-05 callback — канонный адрес по обоим реестрам); «Обучение Стаса» (§2.4: выученный урок с NULL, его ручная правка); «Сериал Карины» — развитие 04-05 по реестру §7 держится теорией (Карина не проходит EXISTS), реплик о ней не нужно.
- **Гейт-заметки:** Владение концептом (§5.7): трёхзначная логика NULL — 03-06; сцена только подводит, юнит по-прежнему напоминает механику коротко («напомним и сразу к выводу» — эта рамка уже есть, сохранить). Обязано выжить: WARNING про scalar-подзапрос с 0 строк (тихий NULL опаснее шумной ошибки «>1 строки»), ASCII-разбор d.id NOT IN (1, NULL), lab-таблица promo с «на всё меню»=NULL — данные и stdout не трогаем, реквизит уже маркетинговый. Стас не получает разноса — бит его арки в том, что он распознаёт NULL сам. Предложение: разметка предлагала вторую сцену на scalar-ловушку «0 строк» — отклонить: WARNING-каллаут уже сильный, микро-диалога открытия достаточно.


#### `04-06-ctes-and-materialization` — ★ Полная сцена

**Участники:** Марат, Даня, Ты.

Ретро спринта отчётов у доски: команда разбирает, что мешало, и последним пунктом всплывает твой отчёт-матрёшка из трёх вложенных подзапросов, который никто не может прочитать. Марат предлагает переписать его именованными шагами — живая мотивация WITH; тут же договариваются собрать ловушки модуля в один лист, и финальный чек-лист юнита становится артефактом этого же ретро. Концовку венчает последняя реплика Дани: «А что будет, когда к этим строкам полезут две кассы разом?» — мостик в модуль 05.

- **Перекур:** Канонический §9.5, перед блоком материализации: Даня приносит миф «выношу подзапрос в CTE — читал, что так быстрее» — Марат: «Читал где?» — «…в статье двенадцатого года» — «Ниже — два плана. Найди разницу». Совмещён с существующей самопроверкой: п.1 «Проверь себя» (безымянный «коллега ждёт ускорения») перефразируется под Даню, ответ-TIP не трогаем.
- **Нити §7:** ★ финал модуля по §6 (ретро + чек-лист как командный артефакт + мостик Дани в 05 — эстафета «несколько сессий» из §1.3 канона подачи); «обещание рекурсивного CTE» 04-02/04-06 → 08-04 — остаётся прозой; перекур — канонический образец §9.5.
- **Гейт-заметки:** ★ по канону — «финал-ретро»; единственный сценовый слот — открытие (§4.1), поэтому ретро ставится в открытие, а финальная таблица ловушек обрамляется прозой как лист с той самой доски — сама таблица и «Что забрать с собой» не переписываются. Обязано выжить: контраст «матрёшка → конвейер WITH» с существующими SQL-примерами (сцена мотивирует, примеры доказывают), правило PG12 (одна ссылка → встраивание; рычаги AS [NOT] MATERIALIZED), тезис «CTE не про скорость, а про структуру», весь чек-лист ловушек 04-01…04-05 (можно одной строкой прозы привязать пункты к героям инцидентов, не меняя таблицу). EXPLAIN — владелец модуль 06 (§5.7): реплика Марата «найди разницу в планах» план не объясняет, юнит уже ссылается прозой — сохранить. Мостик-реплика Дани разрешена §4.1.3 (в юнитах со сценой последняя реплика = крючок); существующий прозаический хендофф в 05 остаётся, реплика его венчает, не заменяет. Гейт 1.5×: заменяются только два вводных абзаца — сцену держать компактной (6–8 реплик).

### 05-transactions-and-mvcc

> Бюджет: 2 полные сцены: 05-05 (тикет Дани «база нестабильна» → «Это контракт. Не сбой» + withRetry) и 05-06 (постмортем дедлока возвратов + мостик Зои с блокнотом). Совпадает с ★ в §6 канона истории для модуля 05 (в бите помечены ровно 05-05 и 05-06); это в пределах бюджета §4.2 «2–3 сцены на модуль». Остальные четыре урока — микро-формы (микро-диалог, две микро-реплики Анны, шапка Зои) плюс два перекура (05-02, 05-04) — квота «~1 на 2–3 урока» соблюдена.


#### `05-01-transactions-and-acid` — Микро-диалог

**Участники:** Анна (в чате), Марат.

Вечерняя сверка не сошлась после перевода выручки касса→касса: Анна рапортует телеграфом в чат («Сверка. Касса-1: списание есть. Касса-2: зачисления нет.»), Марат-финтеховец отвечает коротким вердиктом — деньги не испаряются, между двумя командами есть зазор, и лечится он не аккуратностью, а транзакцией. Существующая сильная проза («испарились/из воздуха», «не баг, а катастрофа») остаётся — микро-диалог обрамляет её, давая инциденту лицо, время и открывая «неделю инцидентов конкурентности».
- **Нити §7:** Эстафета модуля 04 (вопрос Дани на ретро: «что будет, когда к этим строкам полезут две кассы разом?» — §6) принимается прозой открытия; forward dual-write → 09-03 (канон подачи §3, только прозой — §5.9); нить «Транзакционная дисциплина» 03-03 → 05 → 10-01 (канон подачи §3): дисциплина получает теорию.
- **Гейт-заметки:** ★ нет — полная сцена не пишется. Обязаны выжить: сценарий «списание прошло, зачисление упало», формула «вместе или никак», фраза «в системе, которая считает чужие деньги, это не баг, а катастрофа», хендофф в 05-02 (буква I). Анна приносит боль числами и временем (её функция §2.6), но числа демо/stdout в реплики не тащим (§5.4). Виктора не звать: его «Где мои деньги?» отработано в 01-01, дефицит бережём (§2.5); в §6 модуль 05 Виктора не содержит.


#### `05-02-mvcc-mental-model` — Шапка Зои над Заборчиком

**Участники:** Зоя, Даня, Марат.

Открытие-детектив (отчёт по выручке против правки цены капучино) сильное и остаётся прозой; персонаж входит в юнит через шапку Зои над Заборчиком — одной репликой в ревью: «твой забытый BEGIN держит мне VACUUM всей базы». Это канонический setup нити «Спор Зоя/Марат» и естественная точка для голоса DBA в юните про мёртвые кортежи и bloat.

- **Перекур:** После блока xmin/xmax/ctid: Даня — «Погоди. UPDATE не перезаписывает, DELETE не удаляет… база что, только пухнет?» — ответ уводит в мёртвые кортежи и VACUUM и стыкуется с шапкой Зои. Совместить с существующей самопроверкой: вопрос-каллаут задаёт Даня (образец §9.5).
- **Нити §7:** Setup нити «Спор Зоя/Марат» (§7: тлеет с 05-02 «твой забытый BEGIN держит мне VACUUM») → искры в 06-06 → payoff 09-05. Снимок — «общий язык модуля», forward в 05-04/05-05 прозой.
- **Гейт-заметки:** Правило отката §4.3: открытие-проза не заменяется. Безымянный «менеджер», правящий цену, — разовая роль канона подачи §2 и персонаж данных: реплик не получает (§2.9), сцены с ним не строим. 05-02 — владелец MVCC (xmin/xmax/ctid, снимок, bloat) и паттерна session-a/b + \prompt (§5 канона подачи): в шапке и перекуре никто не переобъясняет снимок. Fence про причину escape-hatch и статус эталонного escape-hatch-юнита — сохранить. Формулировку реплики Зои держим по реестру §7 дословно.


#### `05-03-row-locks-and-lost-updates` — Микро-реплика

**Участники:** Анна (в чате).

Строка «через неделю инвентаризация не сходится, и никто не понимает почему» получает голос и дату: чат-рапорт Анны телеграфом с числами («Инвентаризация. Колд брю: по базе минус один. Вторую неделю. Смена не виновата.») встраивается в существующий абзац открытия — по канону §6 это именно её рапорт.
- **Нити §7:** «FOR UPDATE SKIP LOCKED как очередь»: 05-03 вводит примитив и явно откладывает → 09-02 (полная очередь) → 09-03 (relay) — канон подачи §3/§5; forward только прозой. Анна введена в 02-04/02-06 (пилот), склейка со staff-Анной — 04-02 (§10.2); здесь по имени, без переобъяснения роли.
- **Гейт-заметки:** Обязаны выжить: числа боли (оба прочитали 10, оба записали 9; «продали два — остаток упал на один»), паттерн read-modify-write, три починки от простого к общему, механика двух сессий A/B. Бариста за кассами остаются безымянными персонажами данных (§2.9): НЕ именовать их Алисой/Борисом — тёзки вводятся только в 05-04 с разводом по §9.1 канона подачи (разметка ours предлагала «протянуть пару назад» — не делаем, это ломает развод однофамильцев). SKIP LOCKED в репликах не трогаем — владелец 09-02 (§5.7).


#### `05-04-isolation-levels-for-devs` — Микро-реплика

**Участники:** Анна (в чате), Даня, Марат.

Лучшая боль-проза модуля (write-skew: Алиса и Борис одновременно уходят со смены) не заменяется; ущерб получает лицо через чат-рапорт Анны — это её кофейня по §6: «Зал. Пустой. Очередь до двери. Оба бариста на складе.» Одна цитата превращает сломанный инвариант в операционную боль с очередью гостей.

- **Перекур:** После таблицы трёх уровней: Даня — «А давайте „просто“ всё на SERIALIZABLE?» (его мем, §7) — Марат: «Можно. Но больно. Цена — ретраи. Завтра посчитаем» (фирменный вердикт §2.1). Локальный setup с адресом: платит сцена 05-05, где Даня сам ловит 40001. Совместить с существующей самопроверкой, если она стоит в этом слоте.
- **Нити §7:** 05-04 вводит 40001, петля — собственность 05-05 (§5 канона подачи): перекур обещает цену ретраев, но петлю не объясняет. HINT «might succeed if retried» — крючок в 05-05. Мем «просто» (реестр §7, голос Дани). Write-skew — смена кофейни Анны (§6).
- **Гейт-заметки:** Правило отката §4.3 — главное: открытие сценоподобной силы, полная сцена его не заменит; звёзды по канону стоят в 05-05/05-06 правильно, предложений по переносу нет. Внутренний довод бариста («нас двое, я отойду») остаётся прозой-пересказом — НЕ оформлять репликой с подписью: бариста Алиса/Борис — персонажи данных (§2.9), реплик не получают (разметка ours предлагала дать им реплики — запрещено). Обязаны выжить: развод однофамильцев по §9.1 канона подачи, «блокировка 05-03 бессильна: строки разные», таблица трёх уровней с ценой, SSI и «опасная пара».


#### `05-05-retry-on-40001` — ★ Полная сцена

**Участники:** Даня, Марат, Ты.

Утро после включения SERIALIZABLE: в ночных логах россыпь 40001, и Даня приносит на стендап заведённый тикет «база нестабильна» — та самая «роковая ошибка» из текущей прозы, получившая лицо. Марат отвечает катехизисом («Покажи запрос»), вслух читает HINT «The transaction might succeed if retried» и закрывает тикет фирменным вердиктом: «Это контракт. Не сбой.» Ты спрашиваешь, где повторять — неужели в каждом хендлере? — и из вопроса рождается решение: одна командная withRetry. Сцена уходит в теорию юнита: почему повтор в новой транзакции с новым снимком почти всегда проходит за 1–2 попытки.
- **Нити §7:** ★ по §6. «Транзакционная дисциплина» 03-03 → 05 → 10-01 (канон подачи §3); катехизис Марата — рефрен «Покажи запрос» (§7); та же петля → 05-06 (40P01) и 10-01 (капстон применяет, ссылается); доигрывает перекур-гипотезу Дани из 05-04; блокнот Зои толстеет в 05 (§7) — допустимо фоном, без реплики.
- **Гейт-заметки:** Сцена ЗАМЕНЯЕТ прозаический пересказ реакции «показать 500 и отлаживать случайный сбой» (гейт 1.5× по словам, §5.2). Обязаны выжить: связка с Алисой/Борисом из 05-04 и оговорка об однофамильцах — прозой, реплик персонажам данных не давать (§2.9); текст HINT в теории; «контракт двусторонний»; «без ретрай-петли SERIALIZABLE использовать нельзя»; таблица SQLSTATE «что ретраить»; fence про Go-центричный escape-hatch. Владелец петли — сам 05-05: сцена подводит, объясняет теория (§5.7). SQLSTATE и числа stdout в реплики не тащить (§5.4). Финтех-паранойя Марата (§2.1) легально красит вердикт. Даня не громоотвод: тикет — нормальная реакция мидла, функция сцены — нормализация ошибки + встроенный FAQ (§5.1). RU/EN-инвариант числа реплик (§5.10).


#### `05-06-deadlocks-and-advisory-locks` — ★ Полная сцена

**Участники:** Ты, Даня, Марат, Зоя.

Ночью волна 40P01 кладёт обработку возвратов: воркер Дани и твой воркер брали заказ и товар во встречном порядке. Утренний постмортем: Зоя приносит серверный лог deadlock detected и открывает блокнот; каждый защищает свой код — и Марат подтверждает: оба кода по отдельности безупречны, «виноват порядок, не человек». Решение ретро — единый порядок блокировок плюс advisory-лок для пересчётов. Концовка юнита — мостик-реплика Зои, закрывающей блокнот: «Конкуренцию пережили. Теперь — почему один запрос летает, а соседний ползёт» → модуль 06.
- **Нити §7:** ★ по §6 (финал модуля). «War-room пула»: 00-06 → 05-06 «advisory утёк» → 09-04 → 10-04 (§7) — лок живёт на сессии, пул возвращает соединение: держать в теории/Заборчике с адресом 10-04. «Блокнот Зои»: заведён 02-06 → толстеет в 05 → открыт на странице 02-06 в 06-06 → 10-05 (§7) — здесь запись инцидента и реплика-мостик. Ретрай-петля: 40P01 — та же петля, что 40001, ссылка на 05-05 без переобъяснения (§5 канона подачи). Вердикт «виноват порядок, не человек» — callback к правилу постмортема 02-06.
- **Гейт-заметки:** Сцена заменяет обезличенное открытие «два процесса Brew» — процессы получают авторов: Даня и ты, оба безупречны (баланс §2.3: Даня не громоотвод; §3.3: читатель — участник постмортемов, личная ставка сохранена). Обязаны выжить: ASCII-«крест» в теории (сцена на тело теории не залезает — §5.3), «Postgres не зависнет: deadlock_timeout 1 с, жертва получает 40P01», лечение «блокируй в одном порядке» (90%), advisory-лок по 64-битному ключу и кейс «пересчёт кофейни #7», pg_try_advisory_lock без ожидания, fence escape-hatch. Мостик-реплика Зои — дословно по §6; последняя реплика персонажа = крючок (§4.1.3). Гейт 1.5×, RU/EN-инвариант реплик (§5.10).

### 06-indexing-and-explain

> Бюджет: 1 полная сцена — 06-06. Совпадает с §6 канона истории: в бите модуля 06 помечена ровно одна ★ (кульминация-рифма 06-06); общий бюджет §4.2 «2–3 сцены на модуль» здесь сознательно не выбран до потолка (как и в 08 — «плотность сознательно ниже»). Предложение второй сцены (06-01, первый юнит модуля + катехизис-рефрен) зафиксировано в notes урока 06-01 с пометкой «предложение», форма по канону — микро-диалог. Перекуров два (06-02 skip-scan, 06-04 INCLUDE) — в норме «~1 на 2–3 урока» для шести уроков.


#### `06-01-reading-explain-analyze-buffers` — Микро-диалог

**Участники:** Анна (в чате), Ты, Марат.

Вечерний пик: чат-рапорт Анны в духе плашки §9.4 — админка статусов висит, дежурный по тикету — ты. Ты в недоумении («Запрос же тривиальный — одна строка на выходе?»), Марат отвечает катехизисом-рефреном: «Вопрос не что ты спросил, а как база ответила. Покажи план» — и открытие уходит в тему урока: EXPLAIN ANALYZE как алфавит на весь модуль.
- **Нити §7:** «Катехизис Марата» (§7) — развитие: рефрен «что база ответила» звучит здесь, как предписано реестром (setup 00-01, payoff 10-05).
- **Гейт-заметки:** Выжить обязаны числа боли из текущей прозы: миллион строк, две-три секунды, вечерний пик, запрос SELECT * FROM events WHERE ref_no. Rows Removed by Filter и цифры плана остаются в теории/выводе — реплики их не пересказывают (§5.4 канона истории). Внимание: образец плашки §9.4 («кассы, утро, четыре секунды») иллюстрирует ФОРМУ, не факты — рапорт Анны гармонизировать с канонной болью (админка, вечер), второй набор чисел не плодить. Если гейт длины 1.5× жмёт — легальная деградация: плашка + одна микро-реплика Марата. Предложение: 06-01 — кандидат на вторую полную сцену модуля (первый юнит + катехизис-рефрен), если владелец расширит бюджет; по канону ★ здесь нет, поэтому ставлю микро-диалог.


#### `06-02-btree-and-composite-column-order` — Микро-реплика

**Участники:** Стас, Даня, Марат.

Стас приносит акцию с дедлайном — одна цитата в существующем открытии: отчёт «всё за 250 по всем категориям» к обеду, а он ползёт мимо индекса (category, price), хотя «капучино дешевле 500» летал. Разрешение — правило левого префикса в теории юнита.

- **Перекур:** После блока про PG18 skip-scan: Даня читает release notes — «skip-scan спасёт, порядок столбцов больше не важен»; Марат осаживает: «страховка, не проектирование». Существующий ⚠️-каллаут («полагаться на „PG18 сам разберётся“ — плохая ставка») становится вердиктом Марата или остаётся прозой сразу после — готовая пара вопрос→ответ.
- **Нити §7:** «Салфетки Марата» (§7) — развитие: словарь, отсортированный по длине слова, — салфетка 06-02 (setup 01-01, payoff 10-05).
- **Гейт-заметки:** Сильное открытие сохраняется: контраст «летал/полз» и аналогия словаря по длине слова уже в прозе — её не переписывать, а привязать к салфетке Марата одной ремаркой. Index Searches: 9 как отпечаток skip-scan — факт вывода, в реплики не тащить. Юмор — только у Дани (§5.5); Марат — вердикт короче пяти слов. Владение: левый префикс и skip-scan объясняет теория 06-02, Стас лишь приносит заказ (§5.7). Существующий крючок в 06-03 и отсылку вперёд к 06-06 про цену индексов — сохранить.


#### `06-03-when-indexes-dont-help` — Микро-реплика

**Участники:** Стас.

Стас с телефоном экраном к собеседнику (его фирменный реквизит) пересказывает жалобу гостя: Алиса Иванова не может войти — вход по почте тормозит, хотя индекс по email никто не удалял. Загадка «индекс есть, а Seq Scan» уходит в тему non-sargable.
- **Гейт-заметки:** Выжить обязаны: пара Alice@Brew.example / alice@brew.example, загадка «индекс есть, а EXPLAIN показывает Seq Scan», правило «оставь столбец голым», citext-альтернатива в каллауте. Легализовать склейку прозой: Alice@Brew.example — это Алиса Иванова из канона (владелица заказа-якоря #1). Жёсткий гейт §2.9: Алиса — персонаж данных, прямой речи НЕ получает — жалобу пересказывает Стас (его маркер — клиенты по именам). Новых чисел боли не выдумывать (в текущей прозе латентности нет). Владелец non-sargable + индекса по выражению — сам 06-03 (§5 канона подачи), 10-03 потом только применяет; конфликтов владения нет. Форвард-ссылки на модуль 07 (pg_trgm) и 09-05 — оставить прозой.


#### `06-04-partial-covering-and-unique` — Микро-реплика

**Участники:** Зоя, Даня, Марат.

Ты несёшь миграцию с CREATE INDEX ON orders (status) — Зоя в ревью, одной строкой строчными (чат-жанр §4.4): «миллион done в индексе. зачем.» Вопрос без ответа уходит в тему: частичный индекс берёт ровно столько, сколько надо.

- **Перекур:** После блока Index-Only Scan / visibility map: Даня — «а чего мелочиться, кладём в INCLUDE вообще всё»; Марат (или Зоя счётом размера): «ты только что раздул индекс обратно». Функция — аудит скрытого допущения «покрытие бесплатно».
- **Гейт-заметки:** Выжить обязаны: двойная боль (воркер pending — десятки из миллиона done; дашборд ходит в heap за total), Heap Fetches: 0, каллаут про visibility map и VACUUM. UNIQUE связан с модулем 02 ссылкой — не переобъяснять целостность (§5.7); MVCC/VACUUM предполагается пройденным (владелец 05-02). customer_id = 777 в демо/stdout НЕ трогаем (byte-exact gate) — проза может не называть имя клиента; подставлять Бориса Петрова только прозой и осторожно (три тёзки, §9.1 канона подачи) — лучше вовсе без имени. Связку outbox_unpublished_idx → модуль 09 (канон подачи §1.3) в реплики не тащить. Ревью-строка Зои — одна, не диалог: её дефицитная сухость и есть вес.


#### `06-05-gin-for-jsonb-and-arrays` — Микро-реплика

**Участники:** Стас.

Стас запускает акцию «всё с подарком» на главной — одна его цитата в открытии, страница встаёт на 200 тысячах позиций. Привычный CREATE INDEX бессилен (снова Seq Scan) — растерянность разрешается инвертированным индексом GIN.
- **Гейт-заметки:** Выжить обязаны: 200 тыс. позиций, attrs @> '{"gift": true}', драматургия краха привычного приёма («сделал как учили — и снова Seq Scan»), аналогия «предметный указатель» — она остаётся в теле теории, в реплику не переносится (§5.3), fastupdate-каллаут DBA в заборчике. Владение (§5 канона подачи): механика GIN/containment — сам 06-05, операторы доступа — 07-01; Стас ничего не объясняет, только заказывает. Мостик-реплика Стаса «теперь хочу опции в самих заказах» по канону §6 живёт в КОНЦЕ МОДУЛЯ (06-06) — здесь только сеем его аппетит (доволен работающим фильтром); существующий прозаический хук 06-05→06-06 («попробуй выкатить такой индекс на горячую таблицу») — сохранить, он и есть заряд кульминации.


#### `06-06-create-index-concurrently` — ★ Полная сцена

**Участники:** Анна (в чате), Зоя, Марат, Даня, Стас, Ты.

Кульминация-рифма (★ по §6): в обеденный пик на горячую таблицу заказов выкатывают обычный CREATE INDEX — рапорт Анны дословно повторяет 02-06: «Кассы. Обе.» Касса встаёт второй раз за курс: Зоя в war-room убивает построение, молча открывает блокнот на странице инцидента 02-06, между ней и Маратом проскакивают искры «кто виноват в простое», Даня закрывает пророчество 00-01: «Теперь — три». Вечерний разбор с правилом «команда была правильной — способ разрушительным» подводит к CONCURRENTLY (механику объясняет теория юнита); финальная реплика-крючок — Стас: «теперь хочу опции в самих заказах» → модуль 07.
- **Нити §7:** «Это было один раз. — Это было два раза.» (§7) — PAYOFF: «Теперь — три» (Даня); «Блокнот Зои» (§7) — развитие: открыт на странице 02-06; «Спор Зоя/Марат» (§7) — развитие: искры «кто виноват в простое», без разрешения — правило границы рождается только в 09-05.
- **Гейт-заметки:** Единственная ★ модуля по §6 — здесь бюджет и стоит. Выжить обязаны: SHARE-блокировка и очередь INSERT/UPDATE/DELETE, формула «само построение было правильным — способ разрушительным», рифма с 02-06 в секции «Кто кого ждёт», SQLSTATE 25001, indisvalid = false, заборчик про lock_timeout/REINDEX CONCURRENTLY, двухсессионное демо (session-a/b) — сцену в демо и «Запуск» не тащить (§5.3–5.4). Риск гейта 1.5× (§5.2): текущее открытие короткое (~1 абзац) — сцена должна быть компактной (ближе к 6 репликам) либо поглотить смежную прозу боли. Виновник выкатки каноном НЕ назначен (02-06 — читатель, 03-03/07-02 — Даня): предложение — командный релиз без персонального виновника, фокус на «виноват способ, не человек» (правило постмортема 02-06); делать читателя виновником повторно — ослабит арку роста, решение за владельцем. Рапорт Анны — дословная формула «Кассы. Обе.» (время не копировать из 02-06 — там 12:41, здесь обед; сверить с картой модуля 02). Спор Зоя/Марат — только искры, границу НЕ формулировать (владение — 09-05, §5.7). Крючок Стаса — репликой, forward в модуль 07 только прозой (§5.9). CONCURRENTLY объясняет теория (владелец — 06-06; мышление миграций — ссылка на 02-06).

### 07-jsonb-arrays-and-search

> Бюджет: 2 полные сцены (07-01 «поле для всего» и 07-02 «латте за −5») — ровно по ★ §6 канона истории для модуля 07; в пределах бюджета §4.2 (2–3 на модуль). Остальные четыре урока — микро-формы (1 микро-реплика, 3 микро-диалога, все чат-голоса функциональны). Перекура два (07-03 и 07-05) — ~1 на три урока, оба совмещены с существующими самопроверками.


#### `07-01-jsonb-access-and-containment` — ★ Полная сцена

**Участники:** Стас, Марат, Ты.

Стас спускается с телефоном экраном вперёд — гость собрал напиток «овсяное, двойной сироп, без кофеина», и таких комбинаций у каждого своя: «нужно поле для всего» (продолжение его крючка из мостика 06-06). Ты предлагаешь гипотезу новичка — добавить колонки под каждую опцию. Марат выносит канонный вердикт: «Сорок nullable-колонок. Можно. Но больно.» — и сцена уходит в тему юнита: jsonb-колонка `options` и операторы доступа/containment. Финальная реплика Стаса про «а фильтровать по опциям сможем?» цепляет ссылку прозой на GIN из 06-05 (владелец механики — 06-05, тут только применение).
- **Нити §7:** Платит мостик модуля 06 (§6: Стас после GIN-фильтра 06-05 «теперь хочу опции в самих заказах»); нить канона подачи «GIN/containment: 06-05 механика → 07-* применение»; сеет двойной крючок в 07-02 («размер документа не бесплатен»). В реестре §7 канона истории собственной строки нет — новых setup'ов не заводить.
- **Гейт-заметки:** Жёсткое ограничение §2.9: Даня в сцены этого юнита НЕ ставится (фонетическая близость с клиенткой данных Диной). Дина (soy, пустой extras) и Егор (нет ключа milk → NULL) остаются персонажами данных без реплик — их поведение в демо обязано выжить нетронутым. Обязаны выжить: двойной крючок в 07-02 (заборчик «размер документа не бесплатен» + финальный абзац о цене гибкости), техссылка на 03-06 (NULL). Сцена заменяет безличный абзац про «маркетинг запустил кастомизацию», гейт ≤1.5× по словам. Предложение: путаницу -> vs ->> («почему oat в кавычках?») можно оформить репликой Ты в существующей самопровере — опционально, не тратя перекур.


#### `07-02-when-not-to-use-jsonb` — ★ Полная сцена

**Участники:** Даня, Зоя, Марат, Ты, Анна (в чате).

Кульминация модуля и перелом арки Дани. Открытие — рапорт Анны в чате в её телеграф-жанре («Латте на витрине — минус пять рублей. Пробивать?»), след ведёт к идее Дани «переедем карточкой в doc jsonb, так гибче» — никто не может сказать, когда цена стала отрицательной, потому что CHECK внутрь документа не лёг. На постмортеме Даня защищается с цифрами («на тесте летало»), Зоя приносит счёт write-amplification (ссылкой прозой на 05-02, не переобъясняя MVCC), Марат не судит — и Даня сам пишет разбор и выводит правило юнита: «поле с инвариантом — колонка».
- **Нити §7:** Реестр §7: «Мем „просто“» — развитие (реплика Дани «так гибче» — канонная точка нити); «Арка Дани» — развитие (провал → лучший постмортем команды, мост к 10-01). Платит write-amplification из 05-02 (канон подачи §1.3). Правило «виноват способ, не человек» — рефрен постмортем-культуры с 02-06.
- **Гейт-заметки:** Обязаны выжить: инцидент «латте за −5», факт «никто не понял когда», безымянный аналитик сети, который не смог наложить CHECK (роль без реплик — о нём говорят, канон подачи §2), правило «поле с инвариантом — колонка». Владение: MVCC/снимок объяснён в 05-02 — Зоя приводит счёт, но не теорию. Это лучшая боль-сцена модуля по разметке — сцена обязана быть сильнее пересказа задним числом (планка высокая, но материал готовый: есть виновник, разбор и искупление). Заборчик «твой DBA попросит нормализовать поля» отдать голосу Зои естественно — она уже в сцене.


#### `07-03-sql-json-path-and-building` — Микро-реплика

**Участники:** Анна (в чате), Ты, Марат.

Одна цитата в прозе открытия: Анна в чате передаёт запрос тренера бариста — «Тренер спрашивает: в каких рецептах молоко и что тяжелее ста граммов. Новичкам — аллергены». Цепочки -> из 07-01 на вложенных ингредиентах пишутся неуклюже — юнит вводит jsonpath как маршрут по документу и сборку jsonb.

- **Перекур:** После блока про jsonb_set/сборку: Ты — «jsonb_set правит документ прямо в базе?»; Марат — «Возвращает новый. Чем оборачивается перезапись документа целиком — видели на прошлой неделе» (ссылка прозой на 07-02). Совместить с существующей самопроверкой в конвенции каллаутов.
- **Нити §7:** Нитей §7 не задевает; внутримодульная обратная ссылка на 07-02 (цена перезаписи документа) — прозой, не новая нить.
- **Гейт-заметки:** Критично: бариста-тренер — обобщённый персонал канона подачи, реплик НЕ получает (§2.9) — его вопросы приходят чужим голосом (Анна в чате) или косвенной речью; текущая безымянная роль как раз этого и требует. Обязаны выжить: рецепты Латте/Эспрессо/Колд брю в данных, техремарка «JSON_TABLE — это PG17, не PG18» (guardrail точности). Открытие сейчас мягкое, конфликта нет — микро-реплика его достраивает, не заменяя; переход-крючок в 07-04 остаётся прозой.


#### `07-04-arrays-vs-junction-table` — Микро-диалог

**Участники:** Стас, Даня, Марат.

Короткое открытие-спор на ревью: Стас приносит теги для витрины («coffee, hot, limited — к пятнице»), Даня предлагает text[] с цифрами в защиту («компактно, ни одного джойна»), Марат переводит спор в вопрос юнита: «Какие запросы будем задавать?» — и сравнительная таблица массив-vs-junction становится ответом, а не судом.
- **Нити §7:** Нитей §7 не задевает; продолжает применение нити канона подачи «GIN/containment 06-05 → 07-*» (операторы @>/&& на массивах — ссылка прозой). Теги — канонный заказ маркетолога (канон подачи §2: теги/витрины 07/08 — Стас).
- **Гейт-заметки:** Обязана выжить сравнительная таблица моделей — в сцене она рождается как итог спора, это её усиливает. Даня здесь НЕ проигрывает: после перелома 07-02 массив — честно легитимный вариант, вердикт «зависит от запросов» (§2.3: Даня — не громоотвод, ему положены и победы). Заборчик «твой DBA попросит мигрировать массив в junction раньше, чем распухнет» — кандидат на шапку Зои (ступень 5, опционально, решение переписчика). Предложение: триггером спора может стать дубль {coffee, coffee}, который массив молча пропустил, — локальный хук, не новая нить.


#### `07-05-full-text-search` — Микро-диалог

**Участники:** Анна (в чате), Даня, Марат, Стас (в чате, перекур).

Открытие — рапорт Анны в чате: «Новички не находят гайды. Ищут brewing — статья про brew молчит»; Даня: «Тащим Elasticsearch?» — Марат: «Сначала посмотрим, что умеет база» (канонный бит §6). Юнит разворачивает конвейер tsvector/tsquery/ts_rank как ответ базы на тикет.

- **Перекур:** После самого тяжёлого блока (конвейер/ранжирование): Стас в чате — «Люди ищут „limited“ — пусть заголовки весят больше описаний» → живое обоснование setweight; совместить с существующей самопроверкой.
- **Нити §7:** Сеет эстафету «поиск → Elasticsearch / kafka-cookbook» (колонка «сеет» §1.3 канона подачи) — остаётся прозой в заборчике, реплика Дани лишь подводит. Крючок «capucino» в 07-06 — лучший межурочный крючок модуля, сохраняется.
- **Гейт-заметки:** Обязаны выжить: заборчик-эстафета «за этими границами берут Elasticsearch» (прозой, forward — только прозой, §5.9), существующий сильный хендофф в 07-06 (опечатка «капучино vs капучіно») — концовку можно усилить: последний абзац фиксирует новый тикет с «capucino» как событие, без замены прозы. Даня-скепсис здесь только сеется — расплата/полупобеда в 07-06, не дублировать вердикт. Бариста — авторы заметок — остаются персонажами данных без реплик (§2.9).


#### `07-06-pg-trgm-fuzzy` — Микро-диалог

**Участники:** Стас, Даня, Марат, Виктор (в чате).

Развязка поисковой эпопеи: Стас показывает телефон — поисковый запрос гостя «capucino», ноль результатов, гость ушёл без капучино; Даня: «Ну всё, Elasticsearch» — но pg_trgm закрывает тикет за вечер, и скепсис Дани оправдан лишь наполовину (граница инструментов — в матрице выбора). Мостик-концовка: Виктор в чате, увидев работающий поиск, приносит список вопросов «как росла выручка» — крючок в модуль 08.
- **Нити §7:** Мостик модуля §6: Виктор → ★08-01 (подготовка его личного появления и реплики «Я хочу видеть, КАК он рос»); закрывает бит «поисковой эпопеи» 07-05→07-06; эстафета «поиск → соседний курс» остаётся прозой в заборчике/матрице.
- **Гейт-заметки:** Обязаны выжить: матрица выбора инструментов (LIKE/FTS/trgm/внешний движок) — как командный итог эпопеи, и закрывающий абзац «модуль 07 закрыт». Посетитель с опечаткой остаётся безымянным — предложение разметки «отдать опечатку Борису» отклонить: канон подачи §2 фиксирует «посетителя» разовой ролью, а Борис и так трёхкратная тёзка (§9.1 канона подачи). Виктор — только голосом из чата: его личные появления дефицитны (01-01, 08, 10-05), чат-реплика здесь экономит событие для ★08-01. Полной сцены-развязки, которую предлагает разметка, не ставим — ★ в §6 на 07-06 нет; предложение: если бюджет модуля когда-то расширят до 3 сцен, 07-06 — первый кандидат (закрытие модуля — легитимный слот §4.2).

### 08-analytical-and-lateral

> Бюджет: 2 полные сцены — 08-01 (сцена-открытие, достроенная вокруг эталонной прозы по правилу отката §4.3) и 08-06 (сцена-закрытие модуля, приёмка дашборда). Совпадает с двумя ★ в §6 канона истории («Модуль 08 — материал тяжёлый, две полные сцены на модуль, между ними микро-формы»). Остальные четыре урока — микро-формы (микро-реплика, два микро-диалога, плашка); перекуров 2 на 6 уроков (08-03, 08-05) — в квоте «~1 на 2–3 урока», оба совмещены с самопроверкой/мотивацией следующего блока.


#### `08-01-window-basics-partition-order` — ★ Полная сцена

**Участники:** Виктор, Стас, Марат, Ты.

Стас поднимается с отчётом «итог по клиенту», следом в опенспейс лично спускается Виктор — второе живое появление после 01-01. Его каноническая реплика из существующей прозы («Я не хочу знать только итог. Я хочу видеть, КАК он рос… Когда мне стоило прислать ей купон?») становится центром сцены; мотивация — чек заказа #1 в рамке: он помнит, с чего рос бизнес, и хочет видеть, как растёт гость. Ты предлагаешь гипотезу новичка «два запроса и склеить в Go» — Марат подводит: есть способ оставить каждую покупку на месте и доклеить итог рядом; объяснение окон уходит в теорию (владелец — этот юнит). Финальная реплика Виктора «и покажи только тех, кто перевалил за тысячу» — крючок в ограничение «по окну нельзя фильтровать в WHERE» → 08-02.
- **Нити §7:** Нить «Чек заказа #1 в рамке» (§7): развитие «08 — Виктор лично; чек — его мотивация аналитики» кладём именно сюда. Спринт Виктора открывается (payoff — сдача дашборда в 08-06). Мостик из 07 («Виктор увидел поиск и приносит список вопросов») уже подан прозой предыдущего модуля.
- **Гейт-заметки:** Главное — правило отката §4.3: 08-01 — эталон прозаического открытия (канон подачи §6 прямо называет его образцом), сцена ДОСТРАИВАЕТСЯ вокруг живущей в прозе реплики Виктора, не заменяя структуру боли; гейт длины ≤1.5× заменяемой прозы (§5.2). Обязаны выжить: дословная реплика «Я хочу видеть, КАК он рос…», числа отчёта 1270/730/780 (остаются в прозе/данных; существующая дословная реплика Виктора уже содержит 1270 и канонизирована вместе с числом — исключение §5.4 для чисел боли; в новые реплики числа не переносить), формула «GROUP BY схлопнул сами покупки» и абзац «Это не вина маркетолога» (нормализация ошибки). Безымянный «маркетолог» существующей прозы — теперь Стас (маркетолог = Стас по касту). Виктор — ноль жаргона, клиенты — «гости». Реплики «Ты» — 1–3, вопрос/гипотеза без рода. Катехизис-рефрен сюда не тащить — бережём дефицит рефрена (плотность 08-01 и так предельная).


#### `08-02-ranking-and-top-n-per-group` — Микро-реплика

**Участники:** Стас.

Две «безобидные» просьбы открытия получают голос Стаса одной blockquote-цитатой: «max вернул мне число. А мне нужна строка — какой напиток топ-1 в кофе. И если ничья — один: баннер не резиновый». Урок из 04-04 («строка, а не число») он теперь формулирует сам — арка обучения Стаса по §2.4 канона истории.
- **Нити §7:** Арка обучения Стаса: §2.4 прямо фиксирует «к модулю 08 сам формулирует „мне нужна строка, а не число“» — callback к 04-04 прозой; новую строку в реестр §7 не добавляем (развитие нити не оформляем, это эхо характера).
- **Гейт-заметки:** «Аналитик сети» остаётся безымянной ролью прозы и реплик не получает — он не в касте §2 канона истории. Обязаны выжить: ничья 120/120 внутри кофе, `max(units)` → 150 (числа — в прозе, не в реплике Стаса), таблица трёх рангов, тонкость «лишний tie-break в ORDER BY распадает ничью», существующие связки назад в 08-01 и вперёд в 08-03. Предложение: вопрос Стаса о ничьей («кому серебро?») может кормить спортивную метафору rank, но развод row_number/rank/dense_rank остаётся целиком за теорией юнита (владение не мигрирует, §5.7).


#### `08-03-lag-lead-and-frames` — Микро-диалог

**Участники:** Виктор, Даня, Ты.

Детектив двух версий отчёта: Виктор голосом из чата — его канонический маркер «Почему два отчёта — два числа?»; Даня признаётся: «Вторая версия моя. Я просто скопировал первую и поправил» (реплика канонизирована битом §6). Ты кладёшь запросы рядом — diff в одно слово, ROWS/RANGE; сама механика фреймов и дыры 5 февраля объясняется теорией (владелец — этот юнит). Конфликт без злодея: копирование выглядело безобидно.

- **Перекур:** После блока фреймов (самого тяжёлого в модуле): Даня возвращается с аудитом скрытого допущения — «стоп, значит дефолтный фрейм при ORDER BY — RANGE, и мой running total из 08-01 работал не потому, что я его так задумал?». 2–3 реплики; совместить с новой парой самопроверки в конвенции каллаутов (сейчас самопроверки в юните нет) — ответ опирается на существующий буллет Заборчика про RANGE UNBOUNDED PRECEDING.
- **Нити §7:** Признание Дани — эпизод его арки, канонизирован битом §6 модуля 08; реестр §7 арки Дани (03-03 → 07-02 → 10-01) не расширяем — подаём как локальный эпизод. Слово «просто» в реплике оставить бытовым, не оформлять как развитие мема (маршрут мема в §7 — 00-01→02-02/07-02→09-03). Виктор — только чатом: дефицит его живых появлений бережём для 08-01/08-06.
- **Гейт-заметки:** Обязаны выжить: числа 146.67 vs 175.00 и «5 февраля кофейня не работала» (остаются в прозе открытия — реплики чисел не называют, §5.4: персонажи говорят «два числа», «после пятого»), ASCII-схема дыры в ряду, буллет Заборчика про дефолтный фрейм с отсылкой к running total 08-01, хвостовой мостик в 08-04 про вложенные данные. Даню не разносить — Марат в сцене не обязателен; если появляется, то без катехизиса (см. 08-01). Сильная сторона текущего открытия — сухая детективная подача расхождения — сохраняется: микро-диалог встраивается в неё, не заменяя целиком (материал плотный, полной сцене тут не место — ★ в §6 не стоит).


#### `08-04-recursive-ctes` — Микро-диалог

**Участники:** Паша, Зоя.

Второй инцидент открытия (повисший запрос по кольцу маршрутов) получает голоса чат-жанром: Паша (в чате, 23:50, флегматично): «выгрузка маршрутов висит. обычно секунда»; Зоя строчными: «вижу. запрос крутится по кругу. убила» — она находит рекурсию в pg_stat_activity, как велит бит §6. Кольцо Склад→Цех→Кафе→Склад и защита CYCLE объясняются теорией; первая боль (дерево категорий для маркетинга) остаётся прозой.
- **Нити §7:** Payoff нити канона подачи §3 «Обещание рекурсивного CTE для деревьев» (04-02/04-06 → 08-04) — уже стоит в прозе («мы пообещали ещё в 04-06»), сохранить дословно. Блокнот Зои сюда НЕ тащить — его маршрут в §7 (02-06 → 05/06-06 → 10-05) не включает 08-04.
- **Гейт-заметки:** Обязаны выжить: обе боли открытия (дерево категорий + кольцо логиста), секция-fence «Почему этот юнит — на psql, а не на sqlc» (эскейп-хэтч, причина объявлена — не превращать в сцену), схема дерева depth 1–4, формула «якорь плюс шаг». Чат Зои — фирменный стиль строчными (§4.4). «Кто завёл строку Кафе→Склад» никому из каста не атрибуцировать — логист остаётся безымянной разовой ролью канона подачи §2. Военную историю Зои здесь не тратить (лимит «одна на модуль» — и в этом модуле она не нужна). Предложение: реплика Зои «убила» — естественная точка, где читатель впервые видит её работу с pg_stat_activity вживую; связка прозой, без переобъяснения (владение EXPLAIN/планами — модуль 06).


#### `08-05-lateral-joins` — Плашка-сводка

**Участники:** Стас, Ты.

Открытие — плашка-сводка по образцу §9.4: профиль клиента грузится секунду, дашборд топ-покупателей — почти минуту, пул захлёбывается; тикет назначен на тебя. Автор N+1-цикла — «прошлый код», без имени.

- **Перекур:** Между блоками «top-N через CROSS LATERAL» и «LEFT LATERAL сохраняет беззаказных»: Стас с телефоном экраном к тебе — «Опять Карина?! Она пропала из профилей» (payoff по реестру §7); Ты — вопрос-гипотеза («CROSS выбрасывает тех, у кого подзапрос пуст?»). 2 реплики; фикс `LEFT … ON true` остаётся теории. Совмещает передышку с мотивацией следующего блока.
- **Нити §7:** «Сериал Карины» — PAYOFF по реестру §7 (01-02 → 04-01, 04-05 → 08-05: «Стас: „Опять Карина?!“», callback 04-01). Карина — персонаж данных: реплик не получает, о ней говорят (§2.9). Существующая связка с DISTINCT ON 04-04 в хвосте — сохранить.
- **Гейт-заметки:** Обязаны выжить: числа боли (тысяча клиентов — тысяча и один запрос; страница — секунда, дашборд — минута; пул захлёбывается), формула «тело цикла переезжает внутрь базы — один план вместо тысячи round-trip», Заборчик про индекс под условие корреляции, роль Карины как теста LEFT vs CROSS LATERAL. N+1 НЕ атрибуцировать ни Дане (его инциденты канонизированы списком: 03-03, 07-02, копия 08-03), ни читателю (личные ставки читателя — 02-06, 09-05, 10-03; N+1 читателя живёт в 10-03) — здесь «прошлый код», балансировка §2.3/§3.3. По той же причине маркер Дани «на тесте с десятью клиентами летало» тут не использовать. Payoff «Опять Карина?!» — главный момент Стаса в юните: в плашке-открытии его вторым голосом не занимать (репортёр тикета — безлично или саппорт).


#### `08-06-grouping-sets-rollup-cube` — ★ Полная сцена

**Участники:** Виктор, Марат, Ты.

★-сцена стоит не в открытии, а в ФИНАЛЕ юнита — слот «закрытие модуля» из §4.2: команда сдаёт Виктору дашборд, заказанный им в 08-01, — покупки с итогом рядом, топ напитков, тренд, не врущий на дырах, и субитоги одним проходом. Виктор принимает лично (payoff его арки по §2.5 — «личная приёмка дашборда»), говорит про «гостей» без единого технического слова; Марат хвалит скупо, в своём телеграфе. Ты показываешь дашборд — ремаркой-прозой, реплики «Ты» — короткие, без экспертизы. Последняя реплика Виктора — крючок: «а теперь хочу грузить прайс поставщика сам» → мостик в 09-01. Открытие юнита остаётся существующей прозой (UNION ALL-мучение безымянного аналитика).
- **Нити §7:** Payoff спринта Виктора (бит §6: «сдача дашборда Виктору — payoff модуля»); мостик «довольный Виктор просит грузить прайс сам» — setup заказа для 09-01 (бит §6 модуля 09: «файл остатков Паши…, заказ Виктора из 08-06»). Опциональное эхо чека #1 (нить §7 «Виктор лично; чек — его мотивация») — если в 08-01 чек уже прозвучал, здесь только кивком прозой, без повторного развития.
- **Гейт-заметки:** ★ по §6 относится к финальной сцене — открытие НЕ трогаем сверх косметики: UNION ALL-боль с «выручкой категории в колонке магазина» сильна как есть, аналитик остаётся безымянным (не в касте, реплик не получает). Обязаны выжить: квадрат 2×2 свёрнутости, разбор grouping()/level, буллеты «Что забрать с собой», итоговое резюме модуля и перечень тем модуля 09 (forward — только прозой, §5.9): сцена ложится между резюме и анонсом 09, реплика Виктора даёт сюжетный крючок, а существующее «оглавление» модуля 09 сохраняется прозой после. В сцене не цитировать числа stdout (§5.4) — Виктор реагирует на картину, не на цифры. Стас и Даня в сцену не обязательны — каст компактный, дефицитное появление Виктора не разбавляем.

### 09-writes-eventing-and-server-logic

> Бюджет: Полных сцен — 2: 09-03 (dual-write → outbox, гипотеза Тимура) и 09-05 (спор менторов о триггерах). Ровно совпадает со ★ в §6 канона истории для модуля 09 и укладывается в бюджет §4.2 (2–3 сцены на модуль). Остальные три урока — микро-диалоги с чат-голосами (Паша 23:50, Анна-рапорт, зоя-строчными); перекуров два (09-03 exactly-once, 09-05 военная история Зои про IMMUTABLE) — в норме «~1 на 2–3 урока» для пятиурочного модуля, оба совмещены с существующими самопроверками.


#### `09-01-merge-and-copy` — Микро-диалог

**Участники:** Марат, Тимур, Паша (в чате), Ты.

Планёрка начала модуля: Марат представляет Тимура — первого новичка после тебя (включается Акт III), а в чате с 23:50 висит сообщение Паши: файл остатков перестал влезать в ночное окно — «файл как файл, что не так-то?» (заказ Виктора из 08-06 доехал до прода). Тимур с ходу: «Стоп, а почему нельзя гнать инсерты пачками побольше?» — наивный вопрос уводит в COPY и стейджинг-таблицу.
- **Нити §7:** Нить «Тимур» (§7): setup — приходит на планёрке в 09. Прозой — заказ Виктора из мостика 08-06 («грузить прайс поставщика сам»). CSV-мир Паши (§2.7: 09-01 — его канонный юнит).
- **Гейт-заметки:** Выжить обязаны: числа боли (тысячи round-trip, ночное окно, зазор между «прочитал» и «записал»), SKU ESP-01/LAT-01/CAP-01 в демо, развилка-таблица MERGE vs ON CONFLICT, заборчик про DBA и тонкую настройку COPY (кандидат на шапку Зои — ступень 5, по решению переписчика), крючок в 09-02 прозой. Ввод Тимура — один раз по правилу §2: кружка какао одной сухой ремаркой, без обыгрывания. Мем «просто» Тимуру здесь НЕ давать — его наследование канонизировано в 09-03 (§7); маркер Тимура тут — «стоп, а почему». Паша радуется будущим ограничениям — его характер (§2.7). Предложение: ввод первого нового персонажа с 00-01 — событие уровня сцены; если владелец расширит бюджет модуля до 3 сцен, кандидат — здесь; по канону оставлен микро-диалогом.


#### `09-02-skip-locked-job-queue` — Микро-диалог

**Участники:** Стас, Марат, Ты, Зоя (в чате).

Стас с телефоном экраном к тебе: Алисе Ивановой пришло два пуша «заказ готов» и два чека — очередь раздала одну задачу дважды; Марат: «Покажи воркера». Наивная починка через FOR UPDATE ставит воркеров паровозиком — вердикт Марата «Пропусти занятое. Не жди» открывает SKIP LOCKED.
- **Нити §7:** Прямых нитей §7 нет. Из канона подачи §3: «FOR UPDATE SKIP LOCKED как очередь» (05-03 вводит и откладывает → 09-02 полная очередь → 09-03 relay переиспользует). «Покажи запрос/воркера» — допустимый рефрен катехизиса без изменения реестра.
- **Гейт-заметки:** 09-02 — владелец концепта SKIP LOCKED (§5 канона подачи): диалог только подводит, объяснение — в теории. ASCII-диаграмма гонки остаётся в теле теории нетронутой (§5.3 — в открытие не тащить). Реплики не пересказывают stdout-инварианты (total claimed, distinct, duplicates=0). Алиса Иванова — персонаж данных: о ней говорит Стас, сама реплик не получает (§2.9). Выжить: обе боли (двойной пуш И паровозик FOR UPDATE), отсылка «очередь — решение до брокера» (kafka-cookbook), заборчик autovacuum/bloat. Дополнительный чат-голос у заборчика: «зоя (в чате): держи транзакцию короткой» — строчными, функция «парковка правила» (пример прямо из §1.4 канона истории). Виновник кривой очереди — «прошлый код», не Даня и не Тимур (балансировка §2.3).


#### `09-03-transactional-outbox` — ★ Полная сцена

**Участники:** Стас, Тимур, Марат, Ты.

Ночной деплой роняет сервис между INSERT заказа и publish события: утром Стас приносит претензию — дашборд недосчитывает заказы, письмо Борису Петрову не ушло. На разборе Тимур наследует мем: «а давайте просто пошлём в Kafka после INSERT и ретраем?» — Марат стил-мэнит ретраи и распределённые транзакции и отрезает: «Два источника правды. Атомарно не обновить» — так рождается outbox в одной транзакции с заказом. Последняя реплика сцены — крючок: relay вычитывает outbox тем самым SKIP LOCKED, вердикт которого прозвучал в 09-02.

- **Перекур:** После таблицы гарантий доставки: Тимур — «стоп, а почему не exactly-once?»; ответ — дубли глушатся на приёме, processed_outbox_ids; совместить с существующей самопроверкой (вопрос — [!NOTE], ответ — [!TIP]).
- **Нити §7:** Мем «просто» (§7) — payoff: Тимур наследует («просто пошлём в Kafka»). Нить «Тимур» (§7) — развитие: гипотеза-антипаттерн. CDC-эстафета (§7) — setup-точка: 09-03 → 10-05 (открытие publication). Из канона подачи §3: dual-write (05-01 forward → 09-03 outbox → 09-04 контраст → 10-05 CDC).
- **Гейт-заметки:** ★ по §6 — кульминация модуля, сцена по бюджету. Жёсткое правило формулировки §1.4 канона подачи: outbox (уровень приложения) и CDC 10-05 (уровень БД) — ДВА РАЗНЫХ механизма; упоминание Kafka/10-05 в сцене — только прозой, не смешивая. 09-03 — владелец dual-write/outbox (§5): сцена подводит, механика — в теории. Выжить: связка канона orders/outbox/processed_outbox_ids, byte-compat контекст (базовые таблицы совместимы с kafka-cookbook), заборчик про bloat outbox. Гейт §5.2: сцена ЗАМЕНЯЕТ прозаический пересказ dual-write-разрыва, не длиннее 1.5×. Марат — телеграф-вердикты, стил-мэн отвергнутых вариантов (ретрай, 2PC) обязателен. Forward к 10-05 — только прозой (§5.9).


#### `09-04-listen-notify` — Микро-диалог

**Участники:** Анна (в чате), Зоя (в чате), Марат, Ты.

Утренний чат: Анна телеграфом рапортует, что карточки заказов всплывают на экране бариста с опозданием и смена зовёт заказы голосом; ты подкручиваешь частоту опроса — и следом зоя (строчными, без точек) приносит счёт пустых SELECT'ов, жгущих базу. Дилемма «частишь/редишь» разыграна в два чат-голоса; формула Марата «NOTIFY будит, outbox гарантирует» — отдельной микро-репликой у контраст-блока с 09-03, не в открытии.
- **Нити §7:** «War-room пула» (§7) — развитие: 00-06 → 05-06 → 09-04 «NOTIFY ломается» (оговорка про PgBouncer) → 10-04; forward в 10-04 прозой. Из канона подачи §3: dual-write — NOTIFY-контраст. Камео Анны — экран заказов 09-04 канонизирован в §2.6.
- **Гейт-заметки:** Бариста — персонажи данных, реплик НЕ получают (§2.9): боль экрана приносит Анна, защищая смену («смена не виновата, виновата система»). Владелец outbox — 09-03: контраст-блок ссылается прозой, формула Марата не переобъясняет механику (§5.7). Выжить: таймлайн писатель/слушатель (готовая раскадровка — остаётся в теории нетронутой), payload «Эспрессо»/«Латте», оговорка про COMMIT (тишина до коммита), at-most-once и потеря уведомления при перезапуске слушателя. Числа задержки/частоты опроса не выдумывать: взять из существующей прозы дилеммы или оставить качественными; если Анна приносит число — оно канонизируется как число боли (исключение §5.4). Возможная мини-сценка «почему тишина до COMMIT» — не отдельная форма, а ремарка в теле, только если не нарушает §5.3.


#### `09-05-triggers-and-volatility` — ★ Полная сцена

**Участники:** Стас, Зоя, Марат, Ты, Виктор (в чате).

Двойной удар: Стас приносит битый отчёт свежести — след ведёт к твоему коду, где updated_at не проставлен (личная ставка читателя, §3.3), а следом в команду пересылают письмо регулятора: нужен журнал «кто и когда менял цены» (Латте: было 480, стало 500). Вспыхивает первый настоящий спор менторов: Зоя — за триггеры (база переживёт любой ваш код), Марат — против невидимой логики; ты модерируешь наивными вопросами — и при читателе рождается граница «инварианты и журнал — в БД, бизнес-логика — в приложении». Мостик: Виктор (в чате) объявляет запуск программы лояльности → модуль 10.

- **Перекур:** После блока волатильности: военная история Зои (одна на модуль, §2.2) — «IMMUTABLE, который врал»: функцию с враньём в метке искали днями; 2-3 реплики, совместить с существующей самопроверкой.
- **Нити §7:** «Спор Зоя/Марат» (§7) — PAYOFF: тлеет с 05-02, искры в 06-06 → здесь из спора рождается правило границы. Личная ставка протагониста «забытый updated_at — твой» (§3.3 — канонная атрибуция сохранена). Мостик Виктора → 10 (§6). Из канона подачи: RETURNING old/new из 03-05 применяется в AFTER-триггере — ссылкой, не переобъяснением.
- **Гейт-заметки:** ★ по §6 — финал модуля, вторая сцена бюджета. Выжить: аудит «Латте» 480→500, регулятор строго безымянный (новых имён не вводить — реестр §2.9), закрытие модуля перечислением пяти тем + мост в 10 и kafka-cookbook. Гендер-нейтральность личной ставки (§3.2): ремарки настоящим временем («ты находишь свой коммит»), никаких «забыл/забыла»; реплики «Ты» — вопросы-модерация, не экспертиза. Зоя не шутит — аргументирует счётом и случаем; Марат честно стил-мэнит триггеры. Правило границы формулируется в теории юнита ПОСЛЕ сцены — сцена подводит (§5.7). Виктор — только голосом из чата: бережём дефицит его живых появлений (01-01, 08, 10-05). Сцена двухбитовая (updated_at → регулятор) — следить за гейтом 1.5×: часть разбора updated_at можно оставить прозой-связкой между битами. Заборчик про глубину PL/pgSQL и страшилка волатильности остаются; страшилка уходит в перекур-историю Зои.

### 10-use-cases

> Бюджет: Полных сцен — 2 юнита (10-01 и 10-05), что совпадает со звёздами §6 канона истории: ★ 10-01 (планёрка лояльности) и ★★ 10-05 (финал). Двойная звезда 10-05 читается как двойной вес финала — два сценовых слота внутри одного юнита (сцена-открытие с Kafka-командой + сцена-кольцо в концовке), итого ~3 сценовых слота на модуль — в пределах бюджета §4.2 «2-3 полные сцены на модуль». 10-02/10-03/10-04 — микро-формы (ступени 2-3 лестницы §4.3), перекуров два (10-03 и 10-04) на пять тяжёлых капстон-уроков, оба совмещены с существующими самопроверками.


#### `10-01-build-the-brew-schema-capstone` — ★ Полная сцена

**Участники:** Ты, Марат, Даня, Тимур.

Планёрка запуска программы лояльности: два бэкенда будут писать баланс в одну базу — Даня и ты ведёте капстон на равных, Тимур на подхвате. Тимур предлагает «а проверки просто сделаем в Go» (эхо унаследованного мема), Даня — payoff своей арки — сам приводит правило из собственного постмортема 07-02: «поле с инвариантом — колонка», Марат закрепляет вердиктом: инвариант живёт в схеме, ретрай — в клиенте. Сцена заменяет прозаический перечень рисков и уходит в тему капстона: собрать всё выученное курсом в одну схему.
- **Нити §7:** Арка Дани — payoff (со-ведущий капстона, §7); Тимур — развитие (на подхвате, §7); мем «просто» — эхо уже отыгранной нити (payoff был в 09-03), НЕ новый setup; транзакционная дисциплина 03-03→05→10-01 (канон подачи §3).
- **Гейт-заметки:** Обязано выжить: перечень четырёх рисков текущего открытия (float-дрейф на деньгах, дубль email от двух бэкендов, Seq Scan по журналу, гонка «бариста жмёт бонус vs ночной job» с потерей начисления) — как чек-лист планёрки внутри сцены или сразу после прозой; гейт 1.5× по словам жёсткий, открытие уже плотное. Бариста и ночной job — роли/персонажи данных, реплик не получают (§2.9), о гонке говорят герои. «Баланс Алисы 15.00» остаётся в данных/stdout — в репликах чисел stdout нет (§5.4). Владение концептами не мигрирует (§5.7): SERIALIZABLE/ретрай 40001 — 05-05, CHECK — модуль 02, выбор ключа — 01-04/02-01; сцена только подводит, ссылки прозой «как в 05-05». Виктор объявил запуск ещё в мостике 09-05 — лично в 10-01 не появляется (дефицит §2.5), максимум упоминание прозой. Предложение: вторую мини-сцену ночного инцидента с гонкой (из story_opportunity разметки) в бюджет не брать — гонку разыграть в теле теории таймлайном A/B по существующей конвенции.


#### `10-02-price-and-promo-engine` — Микро-реплика

**Участники:** Анна (в чате), Стас.

Рапорт Анны в чате: «Касса. Латте. Две цены. 09:03» — а следом выясняется, что окно промо SUMMER наслоилось само на себя и скидка ушла дважды. Стас, прошедший NULL и ручные правки стоп-листа, сам произносит payoff своей арки: «инвариант — в схему, я себе не доверяю» — и урок уходит в temporal PK WITHOUT OVERLAPS и EXCLUDE.
- **Нити §7:** Обучение Стаса — payoff «инвариант — в схему» (§7); деньги = BIGINT-центы под защитой 01-01→02-04→10-02 (канон подачи §3).
- **Гейт-заметки:** Обязаны выжить обе канонные боли: две цены на один день (завёл менеджер) и наслаивающееся окно промокода SUMMER (двойная скидка/никакая). «Менеджер» остаётся безымянной ролью — виновник называется прозой, реплики не получает. Рапорт Анны — телеграф с временем и числом (маркер голоса §2.6). Аудит «было—стало» для бухгалтерии и RETURNING old/new — прозой: владелец RETURNING — модуль 03, здесь только применение с ссылкой; журнал цен перекликается с 09-05 (письмо регулятора) — упомянуть прозой, не переобъяснять. Риск: не раздуть микро-форму до сцены — звезды у юнита нет, ступень 3 достаточна.


#### `10-03-app-anti-patterns-clinic` — Микро-реплика

**Участники:** Зоя (в чате), Марат, Ты.

Единственная почти-реплика модуля — «запросы простые, ничего не сломано» безымянного DBA — поднимается до именованной цитаты Зои в тикете, строчными: «pg_stat_statements смотрела. запросы простые. база здорова. лечите код.» Дальше открытие остаётся текущей сильной прозой: база здорова, дашборд красный.

- **Перекур:** После тяжёлого блока про N+1: болезнь находится в ТВОЁМ свежем PR — Марат не ругает, а считает round-trip'ы вместе с тобой (2-4 реплики, нормализация ошибки + сократика); совместить с существующей самопроверкой в конвенции каллаутов (вопрос задаёт Марат).
- **Нити §7:** Личная ставка читателя: N+1 в свежем PR — твой (§3.3 канона истории, балансировка Дани §2.3); катехизис Марата — уместен рефрен «что база ответила» (развитие, payoff в 10-05); keyset 03-02 и non-sargable 06-03 — только применение со ссылкой (§5 канона подачи).
- **Гейт-заметки:** Правило отката §4.3 — главное здесь: текущее открытие самое сценичное в модуле, полной сценой НЕ заменять (звезды нет), только дать имя уже существующей реплике DBA — склейка «твой DBA = Зоя» по §2.2 канона истории. Обязаны выжить: метафора «клиника/болезнь/лечение» через весь урок, структура пяти болезней (N+1, SELECT *, non-sargable, глубокий OFFSET, огромный IN), симптомы «сороковая страница падает по таймауту». Пострадавшие коллеги-репортёры (фронтендер и пр.) — безымянные роли, новых персонажей не плодить. Перекур — единственное место диалога в теле: ставить строго на шов после теорблока, не внутрь теории (§5.3).


#### `10-04-pooling-from-the-app` — Микро-диалог

**Участники:** Ты, Зоя (в чате), Марат.

Первое ночное дежурство читателя: после деплоя pgbouncer в 03:11 сыплются алерты — экран заказов молчит, advisory-лок повис, prepared statement падает. Ты пишешь Зое в 03:14 (назывными, без рода: «лок висит. LISTEN молчит.»), она отвечает фирменными строчными: «пул. транзакционный. сессии больше нет.» — чат-жанр §4.4 как открытие вместо обезличенной прозы.

- **Перекур:** Утреннее ретро (2-4 реплики): Зоя формулой «пул даёт бэкенд на транзакцию, не на сессию» подводит к таблице фиксов, Марат переводит в план действий; совместить с существующей самопроверкой «какие три вещи сломались и почему».
- **Нити §7:** War-room пула — payoff: 00-06 (FATAL: too many clients) → 05-06 (advisory утёк) → 09-04 (NOTIFY и пул) → 10-04 (§7); сообщение Зое в 03:14 канонизировано в её арке (§2.2); арка читателя — первое ночное дежурство (§3.3).
- **Гейт-заметки:** Самый «безлюдный» юнит модуля — чат-жанр здесь самый дешёвый и точный инструмент. Обязаны выжить: завязка «Brew вырос, десятки инстансов упёрлись в max_connections» и структура трёх поломок (advisory-локи, LISTEN, prepared statements) — таймлайн одного инцидента их не отменяет, а нанизывает. Сильный мостик в 10-05 («две книги сходятся на одной модели данных») сохранить прозой. Подбор размера пула — территория DBA: заборчик остаётся буллетами, опционально шапка-Зои (ступень 5) по решению переписчика. Гендер-линт: реплики «Ты» в панике — только назывные/настоящее время. 10-04 — владелец темы pgbouncer, поэтому объяснение механики — в теории юнита, реплики Зои лишь подводят (§5.7). Второй перекур модуля — допустимо (5 тяжёлых уроков ≈ 2 перекура).


#### `10-05-the-cdc-seam-handoff` — ★ Полная сцена

**Участники:** Ты, Стас, Тимур, Зоя, Виктор, Марат.

★★ — двойной вес финала: две сцены в одном юните. Открытие: Стас приводит безымянную Kafka-команду за потоком данных; Тимур: «стоп, а почему не outbox с relay, как раньше?» — но отвечаешь уже ты: объяснение PUBLICATION оформляется ремаркой-прозой, а правило byte-compat звучит твоей короткой репликой («переименуем колонку — сломаем им Debezium»). Концовка-кольцо: Зоя делегирует периметр («Слот твой. Следи за диском»), Виктор приносит чек заказа #1 в рамке («первый заказ уезжает в большой мир»), Тимур прибегает со «Стоп, а почему…» — ты: «Покажи запрос»; последняя реплика курса — Марат с итогом года на одной салфетке.
- **Нити §7:** Payoff'ы по реестру §7: катехизис Марата (формулу произносишь ты — Тимуру), блокнот Зои (делегирование периметра), салфетки Марата (итог года), чек заказа #1 («уезжает в большой мир»), обучение Стаса (приводит Kafka-команду), Тимур (его вопрос закрывает кольцо), CDC-эстафета в kafka-cookbook (открывает publication), dual-write 05-01→09-03→09-04→10-05 (CDC заменяет).
- **Гейт-заметки:** ★★ санкционирует отступление от обычного слота: финальная сцена-кольцо живёт в концовке (обычно мостик — проза), итого два сценовых слота в одном юните. Обязано выжить: прямое прощание с читателем («Спасибо, что дошёл до конца»; форма глагола — кандидат ревизии открытого вопроса 2 раздела 11) и мета-рефлексия о SQL-протагонисте — уникальный жанр юнита, сцены его не вытесняют; хук «застрявший слот → No space left on device» — реплика Зои подводит, объяснение остаётся в заборчике/теории. Гости Kafka-команды строго безымянны (межрепозиторных обязательств нет, Debezium — безличная точка стыковки, §6). Вопрос Тимура «стоп, а почему не outbox с relay, как раньше?» (единая формулировка со сценой) — встроенный FAQ; ответ обязан назвать различие механизмов по правилу §1.4 канона подачи (09-03 и 10-05 — два разных способа, не два шага одного), владелец outbox — 09-03, не переобъяснять. Реплики «Ты» — короткие формулы, не лекции (§3.1): развёрнутое объяснение — ремаркой-прозой и теорией. Бариста правит цену, маркетинг публикует статью — источники изменений остаются ролями прозы, без реплик. RU/EN-инвариант числа реплик особенно критичен: сцен две, реплик много.
<!-- SCENE-MAP:END -->

## 10. Фаза 4 — аватары и опциональный интерактив

- [ ] Аватары: брифы готовы в §8 канона (8 персонажей, стиль, палитра
  `#336791`/`#5a9fd4`, реквизит-ключ). Владелец рисует; технические задачи:
  выбрать путь хранения (например, `web/public/story/` или ассеты юнита),
  проверить рендер в обеих темах, размер в тексте урока (иконка у первого
  появления персонажа в 00-01; в остальных сценах — по вкусу владельца).
- [ ] Опционально (Тиер-1 из прошлого плана, движковая работа): компонент
  «реплика с аватаром» в `cookbook-engine` (сейчас сцены — чистый GFM
  blockquote и работают без движковых правок; аватары в тексте возможны
  обычными картинками).

## 11. Открытые вопросы (не блокируют старт)

1. Форма `**You:**` в EN — проверить на EN-пилоте (альтернатива: курсивная
   ремарка вместо прямой реплики).
2. Ревизия маскулинного прошедшего времени о «ты» в существующей прозе всего
   курса (вне сцен): точечные правки или принять как есть — решение владельца
   после пилота. Затрагивает и финальное прощание 10-05 («Спасибо, что дошёл
   до конца») — в карте оно помечено «обязано выжить» как жанр, но форма
   глагола сверяется с этим решением (нейтральный кандидат: «Это был
   последний юнит. Спасибо тебе за этот год в Brew»).
3. Плашка-сводка (ступень 4) — обычная проза или каллаут `[!NOTE]`:
   решить на первом юните с плашкой — `08-05` (волна III), либо раньше на
   `06-01` (волна II), если его микро-диалог деградирует в плашку.
4. Нужен ли Тимуру аватар с первого релиза Акта III или к финалу — вопрос
   художнику.

# Переделка подачи курса PostgreSQL Cookbook (гладкое чтение, без потери byte-exact)

## Overview

Сделать так, чтобы материал курса **читался гладко** (на уровне соседнего
`kafka-cookbook`) и ученик **плавно погружался** в тему — не нарушая жёсткий
byte-exact gate курса. Это переделка **подачи** (README-проза), а не контента и не
кода.

- **Проблема:** по двум workflow-разборам средняя вовлечённость при чтении —
  **Kafka 4.67 / 5 vs Postgres 4.05 / 5**. Курс не «плохой», он хорош *неровно*:
  холодные открытия сильные, но вторая половина почти каждого урока сухая.
- **Главная причина:** код «вываливается» готовым и комментируется задним числом,
  вместо того чтобы **собираться по стадиям**. Поверх — три дешёвых markdown-пробела
  (диаграммы, сравнительные таблицы, «Заборчики» одной стеной) и плоские концовки.
- **Ключевые выгоды:** все правки — **только README-проза**, поэтому byte-exact
  gate проходится дёшево; единый источник правды (`docs/course-canon.md`) убирает
  повторы и рассинхрон; сквозной нарратив превращает плоские концовки в
  story-handoff'ы.
- **Интеграция:** не трогает движок сайта (`@dsbasko/cookbook-engine`), демо-код,
  схемы и вставленный stdout; работает строго в `i18n/{ru,en}/README.md`.

Подход выбран в брейншторме: **Вариант A «Зафиксировать → прокатить»**.

## Context (from discovery)

**Исходники анализа (уже выполнены, в транскрипте сессии):**

- Workflow #1 (16 уроков Kafka+PG + синтез) — диагноз и playbook переносимых
  приёмов; цифры вовлечённости; ранжирование самых сухих уроков (04-01 = 3.5,
  единственный самый сухой; кластер на 4.0).
- Workflow #2 (11 модулей PG + 3 арки спайна + реестр) — каст (37 лиц), сцены,
  зависимости «вводит/предполагает», глоссарий сущностей, правила владения, 7
  нестыковок, поурочный аудит диаграмм/таблиц (диаграмма нужна 43/62, таблица 48/62).

**Источник правды (ГОТОВО): `docs/course-canon.md` (503 строки).** План ссылается
на него как на **вход каждого агента-переписчика**. Ключевые разделы:

- §1 Спайн — линза «Первый год бэкендером в Brew» + якорь «заказ #1» + фон роста;
  помодульные биты 00→10; §1.4 — два разных механизма хендоффа (09-03 outbox ≠
  10-05 CDC-публикация).
- §5 «Кто владеет объяснением» — анти-повтор (владелец объясняет, остальные
  ссылаются).
- §6 Канонический скелет юнита (7 шагов); эталон — **08-01**.
- §7 Playbook (9 приёмов + обязательные артефакты); §8 Правила детерминизма;
  Приложение A — поурочный аудит диаграмм/таблиц.
- §9 Открытые решения к ратификации (см. Task 1).

**Масштаб:** 62 урока × 2 языка (RU+EN) = 124 README. Все 11 модулей живые в
`course.yaml`.

**Релевантные файлы/команды (проверены на диске):**

- Цель правок: `lectures/<module>/<slug>/i18n/{ru,en}/README.md`.
- Эталон-шаблон: `lectures/08-analytical-and-lateral/08-01-window-basics-partition-order/`.
- Пилот (самый сухой): `lectures/04-querying-across-tables/04-01-joins-inner-left-right-full/`.
- `lectures/Makefile`: `make lecture L=<path> [T=db-reset|run|...]`, `make build`,
  `make web-check-coverage`, `make web-generate-readme-toc`.
- Канон Brew: `schema/brew.sql`, `schema/seed.sql` (выверены — см. Task 1).

**Конвенции (из `CLAUDE.md`):** RU-first; логи → stderr, чистый stdout; вставленный
stdout `## Запуск` сверяется gate'ом байт-в-байт; README-TOC между маркерами
`<!-- generated … -->` воспроизводится дословно (truncate по первой запятой).

## Development Approach

- **«Тест» этого плана = per-unit verification gate.** README-проза не покрывается
  unit-тестами; существующие демо-тесты юнитов **не трогаем**. Готовность каждого
  урока подтверждается прогоном gate (см. Verification Strategy). Везде ниже, где
  скилл планирования требует «write tests», читать как **«прогнать gate юнита»**.
- **Атомарная единица — один урок** (пара `i18n/{ru,en}/README.md`). Полностью
  доводим урок до зелёного gate, прежде чем закрывать модуль.
- **Только README-проза.** НЕ трогать: `query.sql`, `cmd/**/main.go`,
  `internal/db/`, `schema.sql`/`db/init.sql`, и вставленный stdout секции
  `## Запуск`. Любое касание запрещённого файла = регрессия gate, откатить.
- **RU-first, затем синхронный EN.** EN зеркалит RU (та же структура, диаграммы,
  таблицы, акценты) — не «новый текст».
- **Маленькие фокусные изменения**, обновлять этот план по ходу (Progress Tracking).
- **Канон — единственный вход.** Каждый агент-переписчик читает `docs/course-canon.md`
  целиком + строку своего урока из Приложения A. Расхождение urok↔канон —
  блокер (⚠️), фиксируем и решаем, канон правим централизованно.
- **Правило эталонов/хорошо-покрытых юнитов.** `08-01` (эталон), `05-02`, `07-01`,
  `03-01` (по Приложению A покрыты) получают **только аддитивные, stdout-нейтральные**
  правки (недостающая диаграмма, расщепление Заборчика буллетами), **никогда**
  структурную переписку. Применяется единообразно ко всем четырём.

## Verification Strategy

> **⚠️ Что gate НЕ ловит автоматически (выяснено plan-review против кода).** Три
> главные риск-точки prose-переписи **не enforced'ятся** существующим gate:
> (1) байт-точность вставленного stdout `## Запуск` — **нет** авто-сверки README с
> `make run` (это ручной шаг); (2) **EN-паритет** — `web-check-coverage` проверяет
> только *наличие* `i18n/en/README.md` (`existsSync`), не контент; (3) **owner-rules**
> §5 — кросс-урочны, никакой инструмент их не сторожит. Существующий gate
> (`db-reset/gen/run/build/coverage`) останется зелёным, **даже если все три
> нарушены**. Поэтому verify ниже делает их **механическими**, а не «на честном
> слове». TOC/заголовок берутся из `course.yaml` и развязаны с прозой README — это
> **не** риск-точка (см. Technical Details).

Для каждого урока (и перед закрытием каждого модуля):

- **Gate-safety — механически (adversarial verify):** в секции `## Запуск` обычно
  **несколько** fenced-блоков (команды ```sh, блок(и) вывода, а у escape-hatch
  двухсессионных юнитов — ещё ASCII-таймлайн/session-блоки).
  1. **Primary (DB-free):** `git diff` урока показывает **ноль изменённых строк
     внутри любого fenced-блока** секции `## Запуск` (ни команды, ни вывод, ни
     таймлайн не переформатированы), и изменения **только** в
     `i18n/{ru,en}/README.md`; запрещённые файлы
     (`query.sql`/`main.go`/`internal/db/`/`schema.sql`) нетронуты. Это основной
     gate, не требует БД.
  2. **Stronger (когда песочница поднята, `docker compose up -d`):** для **того
     конкретного блока, что является stdout демо** (не-`sh`, не-таймлайн
     output-блок) — `make lecture L=<path>` (== `make run`) и байт-сверка stdout с
     этим блоком. Для sqlc-юнитов нужна живая БД; капстоны (модуль 10) `t.Skip` без
     БД.
  3. **Прозо-блоки НЕ перезапускаем:** ASCII-таймлайн / session-блоки (эталон 05-02:
     `xmin=856/863`, «у тебя числа будут другие») намеренно недетерминированы —
     проверяются только git-diff-нулём из п.1, **не** сравнением с `make run`.
- **EN-паритет — структурно:** RU и EN имеют одинаковое число fenced code-блоков,
  одинаковое число таблиц и диаграмм, **одинаковое число заголовков и совпадающую
  последовательность их уровней** (`grep -c '^#'` + последовательность `#`-глубин),
  а **не** дословное совпадение текста заголовков (они переведены: `## Запуск` ↔
  `## Running it`). Семантику ловит ревью, структуру — эта проверка.
- **Верность SQL-стадий:** каждый показанный «стадийный» SQL — честный
  предшественник/подмножество реального `query.sql`; **финальная стадия совпадает с
  тем, что реально исполняется**. Инструментом не проверяется → обязательная
  проверка ревьюера.
- **Faithfulness к канону:** для каждого концепта, который трогает урок, переписчик
  **цитирует строку-владельца §5** и подтверждает «ссылаюсь, не переобъясняю»; скелет
  §6 применён; обязательные артефакты (диаграмма/таблица/буллет-Заборчик/крючок) на
  месте по Приложению A; спайн-хендофф прозой.
- **Детерминизм §8:** нет печати плавающих значений; выигрыши в счётчиках, не
  таймингах; SQLSTATE как факт; двухсессионность — ASCII-таймлайном.
- **Per-unit gate (из CLAUDE.md):** `make lecture L=<path> T=db-reset` идемпотентен;
  `make gen` без diff; `make run` совпадает с README байт-в-байт; `make build`
  зелёный; оба RU+EN присутствуют.
- **Глобально на модуль:** `make web-check-coverage` зелёный; `make
  web-generate-readme-toc` diff-стабилен (тривиально — TOC из `course.yaml`).
- **Rollback (атомарность на уровне урока):** единица отката — один урок (пара
  RU+EN). Если урок не доводится до зелёного — `git checkout` его пары, фиксируем
  ⚠️, **не блокируем остальные уроки модуля**. Модуль помечается `[x]` только когда
  **каждый** его урок прошёл gate индивидуально.

## Progress Tracking

- `[x]` сразу по завершении пункта.
- ➕ — вновь обнаруженные задачи; ⚠️ — блокеры/расхождения urok↔канон.
- Если правка канона затрагивает уже переписанные уроки — добавить ➕-задачу на их
  подтяжку.
- Держать план в синхроне с фактической работой.

## Solution Overview

**Вариант A «Зафиксировать → прокатить»** в трёх фазах:

1. **Фаза 0 (ГОТОВО):** канон `docs/course-canon.md`.
2. **Фаза 0.5:** ратификация §9 — развязка тёзок, имя поля идемпотентности,
   решение по `amount` заказа #1; правки канона/seed/уроков по итогам.
3. **Фаза 1 (пилот):** 04-01 end-to-end по канону+скелету (RU→verify→EN→gate). На
   нём выкристаллизовать **переиспользуемый per-unit чек-лист** и **шаблон
   sweep-workflow**.
4. **Фаза 2 (sweep):** помодульно 00→10. На каждый модуль — Workflow: по агенту на
   урок (RU-перепись по канону) → adversarial verify-проход → мой ревью → EN-синк →
   gate модуля зелёный. Порядок 00→10, чтобы спайн-хендоффы и owner-rules
   накапливались по ходу.

**Почему workflow-оркестрация:** 62 урока — это масштаб, который один контекст не
держит; помодульный fan-out с verify-проходом даёт охват и адверсариальную проверку
gate-safety/faithfulness до моего ревью. Каждый урок остаётся отдельной проверяемой
единицей.

## Technical Details

**Что физически меняется в каждом уроке (README-проза обоих языков):**

1. **dump→build:** `query.sql` подаётся стадиями/диффом в тексте («показываем v1 →
   меняем одну строку → финал»); сам `internal/db/` и stdout не меняются.
2. **«Читаем вывод обратно»** (`Во-первых… Во-вторых…`) по фактам фиксированного
   stdout.
3. **Диаграмма** (ASCII/markdown) — где требует Приложение A (43/62).
4. **Сравнительная таблица/cheat-sheet** — где требует Приложение A (48/62).
5. **«Заборчик» буллетами** вместо запятая-стены.
6. **Концовка-крючок** (story-handoff прозой) вместо плоского указателя оглавления.
7. **Newcomer-вставки**, типографские акценты, steel-man, decision-card — по §7.

**Порядок секций юнита — канонический скелет §6:** боль-сцена → концепт стадиями →
stdout → читаем вывод обратно → код как подтверждение → Заборчик буллетами → крючок.

**Gate-дешевизна и где реальная экспозиция:** поскольку демо-код и stdout не
трогаются, `make run` и его байт-точный вывод остаются валидными. **TOC и заголовок
страницы берутся из `course.yaml`** (движок даже срезает H1 README), а `course.yaml`
мы не трогаем — значит TOC/title **автоматически стабильны** под любой prose-правкой,
и `web-generate-readme-toc` всегда diff-стабилен тривиально. Единственная реальная
gate-экспозиция prose-переписи — **вставленный fenced-блок `## Запуск`**: его нельзя
переформатировать/переотступить (нет авто-сверки — ловится только механической
проверкой из Verification Strategy). Туда и направляем внимание ревьюера.

## What Goes Where

- **Implementation Steps** (`[ ]`): всё, что делается в этом репозитории — правки
  README, канона, seed (если §9 решит «баг»), прогоны gate, workflow-оркестрация.
- **Post-Completion** (без чекбоксов): авторская читка качества прозы (вкусовая),
  возможный повторный workflow-замер вовлечённости, решения, требующие только
  человека.

## Implementation Steps

### Task 1: Фаза 0.5 — ратификация §9 канона и точечные правки канона/seed

**Files:**
- Modify: `docs/course-canon.md` (§9 → решения)
- Modify (по решению автора): `schema/seed.sql` (если `amount` #1 — баг)
- Modify (по решению автора): соответствующие `lectures/04-02-*/i18n/{ru,en}/README.md`, `lectures/05-04-*/…`, `lectures/05-05-*/…` (переименование lab-тёзок)

- [x] получить решения автора по §9 [✓ ратифицировано как гейт-безопасные дефолты; AskUserQuestion отклонён → принял рекомендованные развязки]: (1) имена-замены для двух lab-«Борисов» (staff 04-02, бариста смены 05-04/05) и lab-«Алисы» смены; (2) единое имя поля-ключа идемпотентности (`outbox_id` vs через `processed_outbox_ids`); (3) `amount` заказа #1 = 10.50 vs позиции 9.70 — баг seed (чинить на 9.70) или намеренная денормализация (сделать явным учебным хуком)
- [x] внести решения в `docs/course-canon.md` §9 (пометить ✓ ратифицировано) и в §2/§4, где они отражаются
- [x] НЕ ТРИГГЕРНУТО (§9.7 ратифицирован как намеренная денормализация — 10.50 в замороженном stdout 04-04 + его main.go; seed не трогаем): если «баг seed» — поправить `schema/seed.sql` (`orders` #1 → согласовать с позициями); меняем **только seed-значение**, колонку/тип `amount NUMERIC NOT NULL` в `brew.sql` не трогаем (его и сторожит byte-compat-тест)
- [x] НЕ ТРИГГЕРНУТО (§9.1 ратифицирован как развязка прозой — имена засеяны в query.sql/session-sql/main.go + stdout, переименование гейт-небезопасно): если переименование lab-тёзок — заменить имена только в lab-контексте затронутых уроков (RU+EN), канон `customers` не трогать
- [x] **зафиксировать карту зависимостей §9 как ЖЁСТКИЕ блокеры** (а не advisory): §9.1 (тёзки) блокирует **Task 8** (04-02) и **Task 9** (05-04/05); §9.4 (имя поля идемпотентности) блокирует **Task 7** (03-04) и **Task 13** (09-03); §9.7 (`amount` #1) блокирует **Task 8** (04-02, якорь «чек из четырёх таблиц»). Соответствующий module-sweep НЕ стартует, пока его §9-пункт не ратифицирован
- [x] verify [✓ make build зелёный; ✓ web-check-coverage: 62 урока, RU 62/62, EN 62/62, mismatches 0; ✓ byte-compat brew_test зелёный; уроки не менялись (только docs/course-canon.md) → db-reset/gen/run N/A]: `make build` зелёный; затронутые уроки проходят gate (`db-reset`/`gen`/`run`); `make web-check-coverage` зелёный; (если правился seed) `go test ./lectures/internal/brew/...` — `TestBrewSchema_ByteCompatCanon` зелёный (тест сторожит DDL-токен `amount NUMERIC NOT NULL` в `brew.sql`, но **не** seed-значения → правка значения в `seed.sql` безопасна)

### Task 2: Фаза 1 — пилот 04-01 (JOIN) end-to-end

**Files:**
- Modify: `lectures/04-querying-across-tables/04-01-joins-inner-left-right-full/i18n/ru/README.md`
- Modify: `lectures/04-querying-across-tables/04-01-joins-inner-left-right-full/i18n/en/README.md`

- [x] прочитать канон целиком + строку 04-01 в Приложении A (прочитаны: §1–§9 + Приложение A; строка 04-01 = Венн/таблица-с-NULL + «JOIN → что сохраняет → когда брать»; §9-блокеры 04-01 не связывают — печатается только customer/order_id/status и drink/floor/storage, `amount` нет; тёзок staff/бариста в юните нет, только клиенты)
- [x] RU-перепись по скелету §6: открыть болью «отчёт врёт / Карина пропала» (§3), собрать 4 JOIN-запроса диффом («дописываем одно слово — LEFT»), показать все четыре (доказать «отличается одним словом»), добавить Венн-диаграмму и JOIN-таблицу, Заборчик буллетами, концовку-крючок с callback на Карину; FULL JOIN — steel-man [✓ холодное открытие = промо-список маркетинга молча теряет Карину; диф INNER→LEFT в «Что показывает наш код»; Венн = 3-зонная ASCII-полоса + таблица NULL/«когда брать»; Заборчик = 3 буллета; концовка-callback на Карину + хендофф в 04-02; FULL — steel-man]
- [x] gate-safety: `git diff` показывает изменения только в RU README; stdout `## Запуск` не менялся; `query.sql`/`main.go`/`internal/db/` нетронуты [✓ изменены только `i18n/{ru,en}/README.md`; `## Запуск` fenced-блоки (sh+output) байт-идентичны HEAD по обоим языкам; query.sql/cmd/internal/schema.sql/sqlc.yaml нетронуты]
- [x] EN-синк: зеркалить RU (та же структура/диаграмма/таблица/акценты) [✓ EN зеркалит RU: одинаковая последовательность уровней заголовков (10 шт., [1,2×9]), 6 fenced-блоков, 1 таблица; SQL-блоки идентичны; output-блок = тот же RU stdout + пометка «(The demo prints in Russian.)»]
- [x] прогнать per-unit gate (`db-reset` идемпотентен, `make gen` без diff, `make run` == README, `make build` зелёный, RU+EN на месте, `web-check-coverage` зелёный, TOC diff-стабилен) [✓ db-reset ×2 exit 0; gen без diff в internal/db; live `make run` stdout байт-в-байт == output-блок RU и EN (после отбрасывания make-эхо строки рецепта); build exit 0; web-check-coverage 62/62 RU+EN, mismatches 0; web-generate-readme-toc diff-стабилен]
- [x] зафиксировать в этом плане ⚠️/➕ всё, что пилот выявил про канон или про скелет [см. «Находки пилота 04-01» ниже]

**Находки пилота 04-01 (вход для Task 3):**

- ➕ **Типографика — `—` (тире) НЕ заменять на дефис.** Скилл `writer` требует механической замены em-dash → hyphen, но весь курс (124 README, включая эталон 08-01) и обе оригинальные версии 04-01 используют «—» как корректную русско-/англо-типографику. Замена сломала бы консистентность и пунктуацию; gate прозу не сторожит. **Правило для sweep:** применять содержательные правила `writer` (анти-AI-лексика, ритм, буллеты вместо запятая-стен, sentence-case, без inflated significance), но **em-dash оставлять**. То же для `«елочек»`.
- ➕ **Байт-сверка stdout: отбрасывать make-эхо.** `make lecture L=… T=run` делегирует в суб-`make -C "…" run`, который **печатает строку рецепта** (`make -C "<path>" run`) перед stdout демо. Механическая проверка (a) из Verification Strategy обязана срезать ведущие make-эхо строки до первой строки вывода программы, иначе ложный mismatch. Зафиксировать в чек-листе Task 3.
- ➕ **Венн четырёх JOIN: 3-зонная ASCII-полоса вместо «кругов».** Перекрывающиеся круги в моноширинном ASCII читаются плохо. Рабочий приём: три подписанных бокса (только слева / пересечение / только справа) с занявшими их строками + прозой «INNER берёт среднюю зону, LEFT — среднюю+левую…» + таблица «сохраняет / где NULL / когда брать». Кандидат в playbook §7 (диаграммы).
- ➕ **Скелет §6 — рабочее размещение:** концепт-секции держим прозой; диф-сборку кода (INNER→одно слово→LEFT) концентрируем в «Что показывает наш код» (а не размазываем по концепт-секциям); консолидирующая секция «диаграмма+таблица» идёт **после** четырёх концепт-секций и **перед** кодом. Канон/скелет правок не потребовали — пробелов в §6 пилот не выявил.

### Task 3: Зафиксировать per-unit чек-лист и шаблон sweep-workflow

**Files:**
- Create: `docs/plans/assets/per-unit-rewrite-checklist.md` (короткий, ссылается на канон)
- Modify: `docs/course-canon.md` (§6/§7 — уточнения из пилота, если были)

- [x] выжать из пилота 04-01 переиспользуемый per-unit чек-лист (какие разделы канона прочитать, что сменить, чего НЕ касаться, что проверить) [✓ создан `docs/plans/assets/per-unit-rewrite-checklist.md`: §1 вход (читать), §2 что менять (7 правок по скелету §6 + рабочее размещение из пилота), §3 запрещённые файлы + §9-блокеры, §4 конвенции стиля (em-dash/ёлочки не трогать)]
- [x] включить в чек-лист **механические проверки** из Verification Strategy дословно: (a) извлечь fence `## Запуск` → `make run` → байт-сверка → `git diff` ноль изменений внутри fence; (b) EN-паритет структурно (одинаковые code-блоки/таблицы/диаграммы/заголовки — `grep '^#'`); (c) верность SQL-стадий (финал == реальный `query.sql`); (d) цитирование строки-владельца §5 для каждого тронутого концепта; (e) lesson-level rollback (`git checkout` пары при провале) [✓ §5 чек-листа (a)–(e); все команды проверены на машине (header-seq sed-форма, fence-count `\x60`-awk, table-count `grep -cE`); зафиксирована находка пилота «срезать make-эхо» при байт-сверке]
- [x] добавить в чек-лист **enumeration высокого-риска owner-pairs** для спот-чека на барьере модуля (короткая §5-таблица: NULL→03-06, деньги→01-01, SKIP LOCKED→09-02, MVCC→05-02, keyset→03-02, GIN→06-05, ретрай→05-05, non-sargable→06-03, outbox→09-03) [✓ §7 чек-листа: таблица из всех 9 требуемых пар + EXPLAIN→06; колонка «остальные только ссылаются» для быстрой сверки на барьере]
- [x] зафиксировать шаблон sweep-workflow на модуль: pipeline `RU-перепись (по агенту на урок, читает канон+своя строка Приложения A) → adversarial verify (механический gate-safety + EN-структура + SQL-верность + owner-цитаты + детерминизм) → EN-синк`, с барьером перед закрытием модуля для прогона gate и owner-pair спот-чека [✓ §8 чек-листа: конкретный `pipeline(...)` из 3 стадий + барьер §6 (coverage/TOC/owner-spot-check); порядок модулей 00→10; пометка про opt-in Workflow]
- [x] подтянуть §6/§7 канона, если пилот показал пробел в скелете/playbook [✓ §7 канона: добавлен приём 10 «Множества/Венн в ASCII — подписанными боксами-зонами, не кругами» (нанесён пилотом 04-01); §6 не трогали — пилот не выявил пробела (зафиксировано в находках Task 2)]
- [x] verify: чек-лист самодостаточен (агент по нему + канону переписывает урок без доп. вопросов) [✓ self-review против всех требований Task 3: вход/что-менять/чего-не-касаться/проверки/owner-pairs/sweep-шаблон покрыты; ссылки на канон по §-номерам; путь к канону `../../course-canon.md` корректен; механические команды отработали на машине]

### Task 4: Sweep модуль 00 — getting-connected (00-01…00-05)

**Files:**
- Modify: `lectures/00-getting-connected/00-0{1..5}-*/i18n/{ru,en}/README.md`

- [x] Workflow: по агенту на 00-01…00-05 — RU-перепись по канону+скелету [✓ выполнено напрямую, не Workflow-tool: ralphex-цикл без явного opt-in + gate-критичность → авторинг с централизованной механической проверкой (эквивалент шаблона §8 чек-листа). Аддитивные правки по Приложению A: **00-01** ASCII клиент-сервер + новый `## Заборчик` (его не было); **00-02** диаграмма routing мета/SQL + cheat-sheet 8 команд; **00-03** «два конверта» (контраст склейка↔`$1`); **00-04** cheat-sheet суффиксов `:many`/`:one`/скаляр/`:exec`; **00-05** box «слот=backend» + таблица `Stat()` по шагам. Холодные открытия и stdout не тронуты]
- [x] adversarial verify-проход (gate-safety + faithfulness §5 + детерминизм §8) по каждому уроку [✓ §5a: изменены только 10 README, ноль строк внутри `## Запуск` (секции байт-идентичны HEAD по всем 10); запрещённые файлы нетронуты. §5d owner-rules: деньги→01-01, NULL→03-06, пул/PgBouncer-форвард→10-04 — только ссылками, без переобъяснения; escape-hatch-критерий (владелец — модуль 00) применён в 00-02/00-04. §8: диаграммы/таблицы без плавающих значений; числа `Stat()` = детерминированный stdout]
- [x] мой ревью результатов verify; правки по флагам [✓ единственный флаг — 00-02 live `\dt` ловит чужие lab-таблицы `count_floor`/`count_storage` (из 04-01) в общей песочнице: контаминация окружения, не правка; после их `DROP` — байт-матч. Правок прозы по флагам не потребовалось]
- [x] EN-синк всех уроков модуля [✓ EN зеркалит RU: совпали header-seq + число fenced-блоков + число таблиц по всем 5 (00-01 `[1,2×6]`/8 блоков; 00-02 `[1,2×8]`/6 блоков/10 строк-таблиц; 00-03 9 блоков; 00-04 6 блоков/6; 00-05 6 блоков/5). Диаграммы идентичны, заголовки переведены (`## Заборчик`↔`## The fence`)]
- [x] gate модуля: каждый урок `db-reset/gen/run/build`; `make web-check-coverage` + `make web-generate-readme-toc` зелёные/стабильны; `[x]` по урокам в плане [✓ `make build` exit 0; `make gen` (00-01/00-04) без diff в `internal/db`; live `make run` == README output-блок **байт-в-байт все 5** (00-02 — после очистки 04-01-lab + нормализации хвостовой пустой строки); `db-reset` идемпотентен (повторные прогоны exit 0); `web-check-coverage` 62/62 RU+EN, mismatches 0; `web-generate-readme-toc` diff-стабилен. Уроки **00-01…00-05 — [x]**]

**Находки модуля 00 (вход для остальных sweep'ов):**

- ➕ **Live-run байт-сверка требует canon-only БД для юнитов, печатающих `\dt`/список таблиц.** Общая песочница `brew` накапливает `*_lab`-таблицы других юнитов (поймано: `count_floor`/`count_storage` из 04-01 утекли в `\dt` 00-02 → 11 строк вместо 9). `make db-reset` escape-hatch-юнита накатывает только канон+seed, **не** дропает чужие lab-таблицы. Перед stdout-сверкой такого юнита — чистая БД (`docker compose down -v && up -d`, либо точечный `DROP` чужих lab-таблиц). Не относится к юнитам, чей stdout не перечисляет таблицы.

### Task 5: Sweep модуль 01 — data-types (01-01…01-05)

**Files:**
- Modify: `lectures/01-data-types/01-0{1..5}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 01-01…01-05 (01-01 таблица float/numeric/центы + decision-card; 01-03 ось времени; 01-04 B-tree-локальность + таблица выбора ключа; 01-05 enum/массив/jsonb). Owner-rules: 01-01 владеет «деньги=центы», 01-04 — выбором типа ключа, 01-02 — тизер NULL (полное — в 03-06)
- [ ] adversarial verify-проход по каждому уроку
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 6: Sweep модуль 02 — schema-and-constraints (02-01…02-06)

**Files:**
- Modify: `lectures/02-schema-and-constraints/02-0{1..6}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 02-01…02-06 (02-03 диаграмма «родитель→дети» + таблица ON DELETE; 02-06 таймлайн блокировок + таблица «мгновенный vs переписывающий ALTER»). Owner-rules: 02 владеет словарём SQLSTATE-констрейнтов, 02-06 — мышлением миграций
- [ ] adversarial verify-проход
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 7: Sweep модуль 03 — crud-fluency (03-01…03-06)

**Files:**
- Modify: `lectures/03-crud-fluency/03-0{1..6}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 03-01…03-06 (03-02 keyset диффом против OFFSET + таблица; 03-05 матрица old/new). Owner-rules: 03-02 владеет keyset, 03-06 — полной семантикой NULL (расплата за тизер 01-02)
- [ ] adversarial verify-проход
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 8: Sweep модуль 04 — querying-across-tables (04-02…04-06; 04-01 в пилоте)

**Files:**
- Modify: `lectures/04-querying-across-tables/04-0{2..6}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 04-02…04-06 (диаграммы цепочки JOIN, логического порядка шагов, DISTINCT ON, раскрытия NOT IN; таблицы count-вариантов, EXISTS/IN, материализации). Owner-rules: 04-05 делает callback на NULL 03-06, не переобъясняет
- [ ] adversarial verify-проход
- [ ] мой ревью; правки
- [ ] EN-синк; сверить с уже готовым 04-01 на единство стиля модуля
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 9: Sweep модуль 05 — transactions-and-mvcc (05-01…05-06)

**Files:**
- Modify: `lectures/05-transactions-and-mvcc/05-0{1..6}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 05-01…05-06. Лёгкая рамка спайна на 05-02 (понимание, не «тикет»). 05-02 уже хорошо покрыт (эталон A/B-таймлайна) — трогать минимально. 05-06 диаграмма дедлок-«крест». Owner-rules: 05-02 владеет MVCC, 05-05 — ретрай-петлёй, 05-03 вводит SKIP LOCKED (полное в 09-02)
- [ ] adversarial verify-проход (особое внимание: не сломать детерминизм двухсессионных демо)
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 10: Sweep модуль 06 — indexing-and-explain (06-01…06-06)

**Files:**
- Modify: `lectures/06-indexing-and-explain/06-0{1..6}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 06-01…06-06 (06-01 ASCII-дерево плана «изнутри наружу» рисуем руками + таблица узлов; 06-02 «словарь по длине» + таблица форм запроса; 06-05 GIN + таблица операторов/опклассов). Детерминизм §8: выигрыши в счётчиках/страницах, не таймингах. Owner-rules: 06 владеет EXPLAIN, 06-03 — non-sargable, 06-05 — GIN
- [ ] adversarial verify-проход
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 11: Sweep модуль 07 — jsonb-arrays-and-search (07-01…07-06)

**Files:**
- Modify: `lectures/07-jsonb-arrays-and-search/07-0{1..6}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 07-01…07-06 (07-02 картинка write-amplification; 07-03 jsonpath path-walk; 07-04 таблица массив-vs-junction; 07-05 FTS-конвейер; 07-06 разбиение на триграммы). 07-01 уже покрыт — минимально. Owner-rules: 07-01 владеет операторами доступа jsonb, GIN-обоснование — в 06-05 (ссылка)
- [ ] adversarial verify-проход
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 12: Sweep модуль 08 — analytical-and-lateral (08-01 эталон; 08-02…08-06)

**Files:**
- Modify: `lectures/08-analytical-and-lateral/08-0{2..6}-*/i18n/{ru,en}/README.md`
- (08-01 — позитивный эталон, не переписываем; при желании только добавить недостающую window-диаграмму отдельной мелкой правкой)

- [ ] Workflow: RU-перепись 08-02…08-06 (08-03 ось времени ROWS≠RANGE + таблица; 08-04 дерево категорий; 08-05 картинка N+1/тело цикла; 08-06 квадрат свёрнутости + таблица ROLLUP/CUBE). Держать планку 08-01
- [ ] (опц.) ➕ добавить в 08-01 окно-диаграмму per-customer brackets + расщепить Заборчик — без изменения stdout
- [ ] adversarial verify-проход
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 13: Sweep модуль 09 — writes-eventing-and-server-logic (09-01…09-05)

**Files:**
- Modify: `lectures/09-writes-eventing-and-server-logic/09-0{1..5}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 09-01…09-05 (09-03 диаграмма dual-write-зазора + таблица гарантий; 09-04 таймлайн транзакционности NOTIFY + таблица NOTIFY-vs-outbox; 09-05 OLD/NEW по TG_OP + таблица волатильности). §1.4: 09-03 (outbox) ≠ 10-05 (CDC) — называть различие. Owner-rules: 09-03 владеет outbox, 09-02 — очередью SKIP LOCKED
- [ ] adversarial verify-проход
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 14: Sweep модуль 10 — use-cases / капстоны (10-01…10-05)

**Files:**
- Modify: `lectures/10-use-cases/10-0{1..5}-*/i18n/{ru,en}/README.md`

- [ ] Workflow: RU-перепись 10-01…10-05 (10-02 числовая ось интервалов + таблица temporal-PK vs EXCLUDE; 10-03 диаграмма N+1 + cheat-sheet пяти smells; 10-04 матрица session-vs-transaction-safe; 10-05 сквозной шов-конвейер + сравнение REPLICA IDENTITY). Капстоны «собирают курс» — pays_off ссылками, не переобъясняют. Финал 10-05 закрывает спайн (эстафета в kafka-cookbook)
- [ ] adversarial verify-проход (особое внимание byte-compat 10-05: канон не переименован)
- [ ] мой ревью; правки
- [ ] EN-синк
- [ ] gate модуля зелёный; `[x]` по урокам

### Task 15: Verify acceptance criteria (весь курс)

- [ ] все 62 урока переписаны (RU+EN), каждый прошёл per-unit gate
- [ ] `make build` зелёный по всему workspace; `make web-check-coverage` зелёный; `make web-generate-readme-toc` diff-стабилен
- [ ] спот-чек: ни один `## Запуск` stdout не изменён против исходного; запрещённые файлы (`query.sql`/`main.go`/`internal/db/`/schema) нетронуты по всему диапазону
- [ ] спот-чек faithfulness: owner-rules §5 не нарушены (нет дублирующих объяснений), спайн-хендоффы присутствуют, обязательные диаграммы/таблицы на месте по Приложению A
- [ ] (опц.) повторный workflow-замер вовлечённости на выборке — подтвердить рост против исходных 4.05

### Task 16: [Final] Документация и закрытие плана

**Files:**
- Modify: `CLAUDE.md` (если выявлены новые паттерны подачи)
- Modify: `docs/course-canon.md` (финальная сверка с фактически написанным)
- Move: этот план → `docs/plans/completed/`

- [ ] обновить `CLAUDE.md`, если в ходе sweep'а выкристаллизовались новые паттерны/гочи подачи
- [ ] сверить канон с фактически написанным курсом; зафиксировать расхождения
- [ ] переместить план в `docs/plans/completed/`

## Post-Completion
*Items requiring manual intervention or external systems — informational only*

**Авторская читка качества (вкусовая):**
- Прочитать переписанные уроки «глазами ученика» — гладкость, темп, отсутствие
  AI-маркеров (можно через скилл `writer`/`humanizer` для RU-текста).
- Проверить, что якорь «заказ #1» и спайн-хендоффы читаются естественно, а не
  натянуто (особенно на механических юнитах 00/02/06 — см. §1.5 канона).

**Внешние/ручные проверки:**
- Запустить сайт локально (`make web-dev`) и просмотреть несколько переписанных
  уроков в рендере — диаграммы/таблицы markdown отображаются корректно.
- Решения §9, требующие только автора (имена тёзок, поле идемпотентности, политика
  по `amount` #1), закрываются в Task 1 и **жёстко блокируют** свои module-sweep'ы
  по карте зависимостей из Task 1 (§9.1→Tasks 8/9, §9.4→Tasks 7/13, §9.7→Task 8).

-- schema.sql — DDL-добавки ЭТОГО юнита поверх канона Brew (schema/brew.sql).
--
-- Юниту 04-06 своих таблиц не нужно: CTE-конвейер (траты клиента из позиций) и
-- материализацию (CTE, использованный дважды) показываем на каноне Brew (orders,
-- order_items, customers, read-only). Файл оставлен намеренно: он часть
-- канонической анатомии юнита, и sqlc.yaml всегда перечисляет
-- [../../../schema/brew.sql, schema.sql].

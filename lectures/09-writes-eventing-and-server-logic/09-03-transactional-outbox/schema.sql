-- schema.sql — DDL-добавки ЭТОГО юнита поверх канона Brew (schema/brew.sql).
--
-- Здесь добавок НЕТ: transactional outbox работает на КАНОНИЧЕСКИХ таблицах
-- orders и outbox (+ partial-индекс outbox_unpublished_idx), которые уже
-- определены в schema/brew.sql и байт-совместимы с kafka-cookbook. Именно эта
-- пара «бизнес-факт + событие» едет в capstone 10-05 и дальше — в CDC-эстафету
-- к Kafka-курсу. Заводить под урок свои лабораторные столы значило бы потерять
-- эту связь, поэтому файл намеренно пуст (только этот комментарий).
--
-- sqlc всё равно требует, чтобы файл из списка schema: существовал — поэтому он
-- здесь есть, но всю схему urok'а покрывает канон.

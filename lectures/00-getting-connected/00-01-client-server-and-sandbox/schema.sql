-- schema.sql — DDL-добавки ЭТОГО юнита поверх канона Brew (schema/brew.sql).
--
-- Юниту 00-01 своих таблиц не нужно: первый запрос идёт к версии сервера и к
-- seed-данным канона (drinks). Файл оставлен намеренно, а не удалён: он часть
-- канонической анатомии юнита, и sqlc.yaml всегда перечисляет
-- [../../../schema/brew.sql, schema.sql]. Следующий юнит просто дописывает сюда
-- свой DDL, не трогая конфиг.

-- schema.sql — DDL-добавки ЭТОГО юнита поверх канона Brew (schema/brew.sql).
--
-- Юниту 00-04 своих таблиц не нужно: все запросы идут к каноническим drinks.
-- Файл оставлен намеренно, а не удалён — он часть канонической анатомии юнита,
-- и sqlc.yaml всегда перечисляет [../../../schema/brew.sql, schema.sql]. Юнит,
-- которому понадобится своя таблица, просто допишет её сюда, не трогая конфиг.
